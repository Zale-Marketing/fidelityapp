---
phase: 15-ai-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - trigger/ocio-ai-analyzer.ts
  - trigger/ocio-scraper.ts
autonomous: true
requirements: [OCIO-03, OCIO-04, OCIO-05, OCIO-06, OCIO-07]

must_haves:
  truths:
    - "Ogni nuova recensione con ai_analyzed_at IS NULL viene elaborata da Claude AI"
    - "Ogni recensione analizzata ha sentiment, urgency, themes, is_fake, suggested_reply popolati nel DB"
    - "Lo scraper avvia automaticamente l'analisi AI per il merchant dopo il salvataggio delle recensioni"
    - "Un JSON malformato da Claude non blocca le review successive — viene skippato con log"
    - "Un errore di sistema (ANTHROPIC_API_KEY mancante, DB irraggiungibile) propaga l'eccezione per il retry automatico Trigger.dev"
  artifacts:
    - path: "trigger/ocio-ai-analyzer.ts"
      provides: "Task Trigger.dev 'ocio-ai-analyzer' che riceve { merchantId } e analizza tutte le review non analizzate"
      exports: ["ocioAiAnalyzer"]
    - path: "trigger/ocio-scraper.ts"
      provides: "Scraper aggiornato che chiama tasks.trigger('ocio-ai-analyzer', { merchantId }) dopo ogni merchant scrape riuscito"
  key_links:
    - from: "trigger/ocio-scraper.ts"
      to: "trigger/ocio-ai-analyzer.ts"
      via: "tasks.trigger('ocio-ai-analyzer', { merchantId })"
      pattern: "tasks\\.trigger\\('ocio-ai-analyzer'"
    - from: "trigger/ocio-ai-analyzer.ts"
      to: "ocio_reviews DB"
      via: "UPDATE SET ai_sentiment, ai_score, ai_themes, ai_urgency, ai_is_fake, ai_fake_reason, ai_suggested_reply, ai_analyzed_at"
      pattern: "ai_analyzed_at"
    - from: "trigger/ocio-ai-analyzer.ts"
      to: "https://api.anthropic.com/v1/messages"
      via: "fetch con x-api-key ANTHROPIC_API_KEY, model claude-sonnet-4-5"
      pattern: "api\\.anthropic\\.com"
---

<objective>
Implementa il task Trigger.dev `ocio-ai-analyzer` che analizza ogni nuova recensione Google Maps tramite Claude AI (claude-sonnet-4-5) producendo sentiment, urgenza, temi, rilevamento fake e risposta personalizzata. Lo scraper della Phase 14 viene aggiornato per avviare automaticamente l'analisi dopo ogni batch di recensioni salvate.

Purpose: Chiudere il ciclo scraper → analisi AI, rendendo ogni recensione salvata immediatamente pronta per la dashboard della Phase 16 con tutti i campi `ai_*` popolati.
Output: trigger/ocio-ai-analyzer.ts (nuovo) + trigger/ocio-scraper.ts (aggiornato con trigger dell'analisi)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-intelligence/15-CONTEXT.md
@.planning/phases/14-scraping-pipeline/14-01-SUMMARY.md

<interfaces>
<!-- Tipi esistenti in lib/types.ts da usare direttamente -->

```typescript
// lib/types.ts — OCIO section
export type OcioConfig = {
  id: string
  merchant_id: string
  google_maps_url: string | null
  place_name: string | null
  business_description: string | null
  reply_tone: 'professional' | 'warm' | 'formal'
  module_reviews: boolean
  // ... altri campi
}

export type OcioReview = {
  id: string
  merchant_id: string
  review_id: string
  author_name: string | null
  rating: number | null
  text: string | null
  published_at: string | null
  ai_sentiment: 'positive' | 'neutral' | 'negative' | null
  ai_score: number | null
  ai_themes: string[] | null
  ai_urgency: 'low' | 'medium' | 'high' | 'critical' | null
  ai_category: string | null
  ai_is_fake: boolean
  ai_fake_reason: string | null
  ai_suggested_reply: string | null
  ai_analyzed_at: string | null
  // ... altri campi
}
```

<!-- Pattern Trigger.dev task (da trigger/ocio-scraper.ts per confronto) -->
```typescript
import { logger, task } from "@trigger.dev/sdk/v3"
// task() per task avviati on-demand (NON schedules.task che è per cron)
export const myTask = task({
  id: "my-task-id",
  maxDuration: 300,
  run: async (payload: { merchantId: string }) => {
    // ...
  },
})
```

<!-- Pattern chiamata Anthropic (da app/api/whatsapp/ai-test/route.ts) -->
```typescript
const res = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': apiKey,          // process.env.ANTHROPIC_API_KEY
    'anthropic-version': '2023-06-01',
  },
  body: JSON.stringify({
    model: 'claude-sonnet-4-5',   // locked
    max_tokens: 500,              // locked
    system: systemPrompt,
    messages: [{ role: 'user', content: userMessage }],
  }),
})
const data = await res.json()
// data.content[0].text contiene la risposta testuale
```

<!-- Pattern DB nei task Trigger.dev (da trigger/ocio-scraper.ts) -->
```typescript
import { createClient, SupabaseClient } from "@supabase/supabase-js"
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const supabase = createClient<any, "public", any>(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)
```

<!-- Pattern tasks.trigger per avviare task on-demand dallo scraper -->
```typescript
import { tasks } from "@trigger.dev/sdk/v3"
await tasks.trigger('ocio-ai-analyzer', { merchantId })
// fire-and-forget: non await il risultato, non blocca lo scraper
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Creare trigger/ocio-ai-analyzer.ts</name>
  <files>trigger/ocio-ai-analyzer.ts</files>
  <action>
Creare il file `trigger/ocio-ai-analyzer.ts` come Trigger.dev `task` (NON `schedules.task` — questo task è on-demand, avviato dallo scraper).

**Struttura del task:**

```typescript
import { logger, task } from "@trigger.dev/sdk/v3"
import { createClient } from "@supabase/supabase-js"

export const ocioAiAnalyzer = task({
  id: "ocio-ai-analyzer",
  maxDuration: 600,  // 10 min — potenzialmente 50 review * ~3s/call
  run: async (payload: { merchantId: string }) => {
    // ...
  },
})
```

**Logica run:**

1. Valida `ANTHROPIC_API_KEY` — se mancante, `throw new Error(...)` (Trigger.dev gestisce retry)
2. Crea supabase client con service role key (stesso pattern di ocio-scraper.ts)
3. Fetch config merchant: `SELECT place_name, business_description, reply_tone FROM ocio_config WHERE merchant_id = merchantId`
4. Fetch reviews non analizzate: `SELECT id, text, rating FROM ocio_reviews WHERE merchant_id = merchantId AND ai_analyzed_at IS NULL`
5. Se nessuna review: logga e return early
6. Loop sequenziale su ogni review:
   - Chiama `analyzeReview(review, config, apiKey)` — funzione helper interna
   - Su successo: UPDATE `ocio_reviews` con tutti i campi ai_* + `ai_analyzed_at = now()`
   - Su errore JSON parse: `logger.error(...)`, skip (non throw), continua
7. Logga summary finale: `{ merchantId, processed: N, skipped: M, errors: K }`

**Funzione helper `analyzeReview`:**

Costruisce il prompt di sistema:
```
Sei un esperto di analisi delle recensioni online. Analizza la recensione e rispondi SOLO con un oggetto JSON valido, senza markdown, senza backtick.
```

Messaggio utente (include tutti i contesti locked dalla CONTEXT.md):
```
Recensione da analizzare:
Rating: {rating}/5
Testo: "{text}"

Informazioni attività:
- Nome/descrizione: {business_description || place_name || "attività locale"}
- Tono risposta desiderato: {reply_tone} (professional = formale ma accessibile, warm = caldo e personale, formal = molto formale)

Regole:
- "themes": array di keyword brevi IN ITALIANO (max 4-5 parole singole, es. ["servizio", "attesa", "qualità"])
- "suggested_reply": scrivi nella STESSA LINGUA della recensione
- "is_fake": true SOLO se ci sono segnali chiari (testo generico copiato, non pertinente, pattern spam) — preferisci false in caso di dubbio
- "score": 1-10, intensità/impatto della recensione (non solo sentiment — es. review positiva molto entusiasta = 9, negativa grave = 9, positiva tiepida = 4)
- "urgency": "critical" solo per minacce legali/sanitarie/sicurezza, "high" per problemi gravi ripetuti, "medium" per insoddisfazione standard, "low" per feedback positivi/neutri

Rispondi SOLO con questo JSON (nessun testo extra):
{
  "sentiment": "positive" | "neutral" | "negative",
  "score": 1-10,
  "urgency": "low" | "medium" | "high" | "critical",
  "themes": ["keyword1", "keyword2"],
  "is_fake": true | false,
  "fake_reason": "stringa esplicita o null",
  "suggested_reply": "testo risposta"
}
```

**Chiamata Anthropic** (usa il pattern esatto da ai-test/route.ts):
- URL: `https://api.anthropic.com/v1/messages`
- Header: `x-api-key`, `anthropic-version: 2023-06-01`
- Model: `claude-sonnet-4-5` (locked)
- max_tokens: 500 (locked)
- system: il prompt di sistema
- messages: `[{ role: 'user', content: userMessage }]`
- Leggi risposta da `data.content[0].text`

**Parse JSON:** Usa `JSON.parse(responseText.trim())`. Se fallisce, `logger.error` con review id e raw response, incrementa contatore `errors`, `continue`.

**UPDATE DB per review analizzata con successo:**
```typescript
await supabase
  .from("ocio_reviews")
  .update({
    ai_sentiment: parsed.sentiment,
    ai_score: parsed.score,
    ai_themes: parsed.themes,
    ai_urgency: parsed.urgency,
    ai_is_fake: parsed.is_fake,
    ai_fake_reason: parsed.fake_reason,
    ai_suggested_reply: parsed.suggested_reply,
    ai_analyzed_at: new Date().toISOString(),
  })
  .eq("id", review.id)
```

**Gestione review con testo molto corto (Claude's discretion):**
- Se `review.text` è null o lunghezza < 5 caratteri (es. solo emoji), includi comunque nel prompt con testo "N/A" o il testo così com'è. Claude gestisce il caso gracefully con il rating disponibile.
- Non skippare questi casi — Claude può comunque assegnare sentiment dal rating.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `trigger/ocio-ai-analyzer.ts` esiste ed esporta `ocioAiAnalyzer`
    - Task id: `'ocio-ai-analyzer'`
    - Zero errori TypeScript
    - Funzione `analyzeReview` helper interna con prompt completo
    - Loop sequenziale con error isolation (skip su JSON malformato, throw su errori di sistema)
    - Logga summary `{ merchantId, processed, skipped, errors }` alla fine
  </done>
</task>

<task type="auto">
  <name>Task 2: Aggiornare trigger/ocio-scraper.ts per avviare l'analisi AI</name>
  <files>trigger/ocio-scraper.ts</files>
  <action>
Modificare `trigger/ocio-scraper.ts` per importare `tasks` da `@trigger.dev/sdk/v3` e chiamare `tasks.trigger('ocio-ai-analyzer', { merchantId })` dopo ogni merchant scrape riuscito.

**Import da aggiungere in cima al file:**
```typescript
import { logger, schedules, tasks } from "@trigger.dev/sdk/v3"
```
(aggiungere `tasks` all'import esistente di `logger, schedules`)

**Modifica della funzione `scrapeForMerchant`:**
La funzione NON viene modificata — il trigger avviene nel loop principale.

**Modifica del loop principale in `ocioReviewScraper.run`:**
Dopo `await scrapeForMerchant(merchantId, googleMapsUrl, supabase)` + `successCount++`, aggiungere:

```typescript
// Avvia analisi AI per questo merchant (fire-and-forget)
try {
  await tasks.trigger('ocio-ai-analyzer', { merchantId })
  logger.log("ocio-ai-analyzer triggered", { merchantId })
} catch (triggerErr) {
  // Non bloccare lo scraper se il trigger fallisce
  logger.error("failed to trigger ocio-ai-analyzer", {
    merchantId,
    error: triggerErr instanceof Error ? triggerErr.message : String(triggerErr),
  })
}
```

Il trigger è wrappato in try/catch separato: un fallimento del trigger non conta come fallimento dello scraper — `successCount` rimane incrementato, lo scraper continua normalmente.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `trigger/ocio-scraper.ts` importa `tasks` da `@trigger.dev/sdk/v3`
    - `tasks.trigger('ocio-ai-analyzer', { merchantId })` chiamato dopo ogni merchant scrape riuscito
    - Il trigger è in try/catch separato (non propaga l'errore allo scraper)
    - Zero errori TypeScript
    - Build passa
  </done>
</task>

</tasks>

<verification>
Dopo il completamento di entrambi i task:

1. `npx tsc --noEmit` — zero errori TypeScript
2. `npm run build` — build completa senza errori
3. `grep -n "ocio-ai-analyzer" trigger/ocio-scraper.ts` — conferma che lo scraper chiama il task AI
4. `grep -n "export const ocioAiAnalyzer" trigger/ocio-ai-analyzer.ts` — conferma export corretto
5. `grep -n "ai_analyzed_at" trigger/ocio-ai-analyzer.ts` — conferma UPDATE del campo sentinel
</verification>

<success_criteria>
1. `trigger/ocio-ai-analyzer.ts` esiste, esporta `ocioAiAnalyzer`, id task = `'ocio-ai-analyzer'`
2. Task chiama Claude API con model `claude-sonnet-4-5`, max_tokens 500, response JSON con i 7 campi locked
3. Task processa tutte le reviews con `ai_analyzed_at IS NULL` per il merchant ricevuto nel payload
4. UPDATE atomico per singola review: `ai_sentiment, ai_score, ai_themes, ai_urgency, ai_is_fake, ai_fake_reason, ai_suggested_reply, ai_analyzed_at`
5. JSON malformato: logger.error + skip, NON throw
6. `ANTHROPIC_API_KEY` mancante: throw (retry automatico Trigger.dev)
7. `trigger/ocio-scraper.ts` aggiornato: chiama `tasks.trigger('ocio-ai-analyzer', { merchantId })` dopo ogni merchant scrape riuscito, in try/catch separato
8. Zero errori TypeScript, build passa
9. Summary finale loggata: `{ merchantId, processed: N, skipped: M, errors: K }`
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/15-ai-intelligence/15-01-SUMMARY.md` seguendo il template standard.
</output>
