---
phase: 13-ocio-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - components/dashboard/Sidebar.tsx
  - app/dashboard/settings/page.tsx
  - app/dashboard/ocio/page.tsx
autonomous: true
requirements:
  - SET-03

must_haves:
  truths:
    - "Sidebar mostra voce 'OCIO' con icona Eye solo se il merchant ha piano business"
    - "Voce OCIO in sidebar linka a /dashboard/ocio"
    - "Pagina /dashboard/settings contiene card/link per navigare a /dashboard/ocio/settings"
    - "Merchant FREE o PRO che accede direttamente a /dashboard/ocio vede UpgradePrompt BUSINESS"
  artifacts:
    - path: "components/dashboard/Sidebar.tsx"
      provides: "Sidebar con voce OCIO condizionale"
      contains: "isBusiness.*Eye.*OCIO\|OCIO.*isBusiness"
    - path: "app/dashboard/settings/page.tsx"
      provides: "Settings page con link a OCIO"
      contains: "ocio/settings"
    - path: "app/dashboard/ocio/page.tsx"
      provides: "Stub dashboard OCIO con gating BUSINESS"
      min_lines: 20
  key_links:
    - from: "components/dashboard/Sidebar.tsx"
      to: "/dashboard/ocio"
      via: "Link condizionale su isBusiness"
      pattern: "isBusiness.*href.*ocio\|href.*ocio.*isBusiness"
    - from: "app/dashboard/settings/page.tsx"
      to: "/dashboard/ocio/settings"
      via: "Link card nella sezione Integrazioni"
      pattern: "ocio.*settings\|ocio/settings"
---

<objective>
Integra il modulo OCIO nella navigazione dashboard: voce sidebar condizionale per merchant BUSINESS, link nella pagina settings, e pagina stub /dashboard/ocio con gating BUSINESS (placeholder che Phase 16 popolera con la dashboard completa).

Purpose: SET-03 richiede che tutte le entry point OCIO siano gated BUSINESS. La sidebar deve essere coerente con il pattern esistente (showWaExtras) ma con logica isBusiness invece di isPro+waConnected.
Output: Sidebar aggiornata, settings page con link OCIO, stub /dashboard/ocio/page.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ocio-foundation/13-CONTEXT.md
@.planning/phases/13-ocio-foundation/13-01-SUMMARY.md

<interfaces>
<!-- Sidebar.tsx attuale — pattern completo da leggere prima di modificare -->
<!-- Il file e in components/dashboard/Sidebar.tsx -->
<!-- Pattern esistente showWaExtras: -->
```typescript
// Fetch esistente in loadMerchantStatus():
const { data: merchant } = await supabase
  .from('merchants')
  .select('plan, sendapp_status')
  .eq('id', profile.merchant_id)
  .single()

if (merchant) {
  const plan = (merchant.plan as string)?.toLowerCase() ?? 'free'
  setIsPro(plan === 'pro' || plan === 'business')
  setWaConnected(merchant.sendapp_status === 'connected')
}

const showWaExtras = isPro && waConnected
```

<!-- Aggiungere isBusiness state analogamente: -->
```typescript
// Aggiungere:
const [isBusiness, setIsBusiness] = useState(false)
// In loadMerchantStatus() dopo il set del piano:
setIsBusiness(plan === 'business')
// Poi usare:
const showOcio = isBusiness
```

<!-- Sezione WhatsApp nella sidebar (pattern da replicare per OCIO): -->
```tsx
{showWaExtras && (
  <>
    <div className="pt-2 pb-1 px-3">
      <p className="text-white/30 text-xs font-medium uppercase tracking-wide">WhatsApp</p>
    </div>
    {[...].map(...)}
  </>
)}
```

<!-- UpgradePrompt per il stub /dashboard/ocio/page.tsx -->
```typescript
<UpgradePrompt feature="Dashboard OCIO — Reputation Intelligence" requiredPlan="BUSINESS" />
```

<!-- Settings page — sezione Integrazioni esistente (da aggiungere sotto i link WA e Webhook): -->
```tsx
// Pattern link card esistente in settings/page.tsx:
<Link href="/dashboard/settings/whatsapp" className="flex items-center justify-between p-4 border border-[#E8E8E8] rounded-[8px] hover:bg-[#F9F9F9] transition-colors">
  <div className="flex items-center gap-3">
    <div className="w-9 h-9 rounded-[8px] bg-green-50 flex items-center justify-center">
      <MessageCircle size={18} className="text-green-600" />
    </div>
    <div>
      <p className="text-sm font-medium text-gray-900">WhatsApp Marketing</p>
      <p className="text-xs text-gray-500">Connetti il tuo numero e invia messaggi</p>
    </div>
  </div>
  <span className="text-gray-400 text-sm">›</span>
</Link>
```

<!-- Icone Lucide disponibili in sidebar (gia importate): Home, Target, Users, Layers, Bell, BarChart2, CreditCard, Settings, ScanLine, MessageCircle, Bot -->
<!-- Aggiungere Eye per OCIO -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere voce OCIO a Sidebar.tsx</name>
  <files>components/dashboard/Sidebar.tsx</files>
  <action>
    Leggere components/dashboard/Sidebar.tsx prima di modificare.

    Modifiche da applicare:

    1. Aggiungere `Eye` agli import da lucide-react (gia presenti: Home, Target, Users, Layers, Bell, BarChart2, CreditCard, Settings, ScanLine, MessageCircle, Bot).

    2. Aggiungere stato isBusiness:
    ```typescript
    const [isBusiness, setIsBusiness] = useState(false)
    ```

    3. In loadMerchantStatus(), dopo `setIsPro(...)` e `setWaConnected(...)`, aggiungere:
    ```typescript
    setIsBusiness(plan === 'business')
    ```

    4. Dopo la sezione `{showWaExtras && (...)}` (sezione WhatsApp), aggiungere la sezione OCIO:
    ```tsx
    {isBusiness && (
      <>
        <div className="pt-2 pb-1 px-3">
          <p className="text-white/30 text-xs font-medium uppercase tracking-wide">OCIO</p>
        </div>
        {[
          { href: '/dashboard/ocio', icon: Eye, label: 'OCIO' },
        ].map(({ href, icon: Icon, label }) => {
          const isActive = pathname.startsWith(href)
          return (
            <Link
              key={href}
              href={href}
              className={`flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-colors ${
                isActive
                  ? 'bg-[#2A2A2A] text-white'
                  : 'text-white/70 hover:bg-[#1E1E1E] hover:text-white'
              }`}
            >
              <Icon size={18} />
              {label}
            </Link>
          )
        })}
      </>
    )}
    ```

    Non modificare nient'altro in Sidebar.tsx — mantenere NAV_ITEMS, showWaExtras, bottone scanner invariati.
  </action>
  <verify>grep -n "isBusiness\|Eye\|ocio" /c/Users/Zanni/fidelityapp/components/dashboard/Sidebar.tsx</verify>
  <done>
    - Eye importato da lucide-react
    - isBusiness state e setIsBusiness presenti
    - Sezione OCIO condizionale con link /dashboard/ocio appare dopo sezione WhatsApp
    - Solo merchant con plan='business' vede la sezione OCIO nella sidebar
  </done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere link OCIO a settings/page.tsx + creare stub /dashboard/ocio/page.tsx</name>
  <files>app/dashboard/settings/page.tsx, app/dashboard/ocio/page.tsx</files>
  <action>
    **Parte A — app/dashboard/settings/page.tsx:**
    Leggere il file prima di modificare.

    Aggiungere `Eye` agli import da lucide-react (gia presenti: MessageCircle, Zap).

    Nella sezione "Integrazioni" (div con i due Link a whatsapp e webhooks), aggiungere un terzo link card OCIO DOPO il link Webhook:
    ```tsx
    <Link
      href="/dashboard/ocio/settings"
      className="flex items-center justify-between p-4 border border-[#E8E8E8] rounded-[8px] hover:bg-[#F9F9F9] transition-colors"
    >
      <div className="flex items-center gap-3">
        <div className="w-9 h-9 rounded-[8px] bg-purple-50 flex items-center justify-center">
          <Eye size={18} className="text-purple-600" />
        </div>
        <div>
          <p className="text-sm font-medium text-gray-900">OCIO — Reputation Intelligence</p>
          <p className="text-xs text-gray-500">Monitora recensioni Google Maps con AI (piano BUSINESS)</p>
        </div>
      </div>
      <span className="text-gray-400 text-sm">›</span>
    </Link>
    ```

    Mostrare il link a tutti i piani (il gating avviene nella pagina /dashboard/ocio/settings stessa). Non condizionare sulla visibilita qui.

    **Parte B — app/dashboard/ocio/page.tsx:**
    Creare il file come stub con gating BUSINESS. Phase 16 sostituira questo file con la dashboard completa delle recensioni.

    ```typescript
    'use client'

    import { usePlan } from '@/lib/hooks/usePlan'
    import UpgradePrompt from '@/components/ui/UpgradePrompt'
    import Link from 'next/link'
    import { Eye, Settings } from 'lucide-react'

    export default function OcioDashboardPage() {
      const { isBusiness, loading } = usePlan()

      if (loading) {
        return (
          <div className="flex items-center justify-center min-h-[200px]">
            <div className="w-6 h-6 border-2 border-black border-t-transparent rounded-full animate-spin" />
          </div>
        )
      }

      if (!isBusiness) {
        return (
          <div className="max-w-2xl mx-auto py-8">
            <UpgradePrompt feature="Dashboard OCIO — Reputation Intelligence" requiredPlan="BUSINESS" />
          </div>
        )
      }

      return (
        <div className="max-w-3xl mx-auto space-y-6">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-bold text-gray-900 flex items-center gap-2">
                <Eye size={24} />
                OCIO
              </h1>
              <p className="text-gray-500 text-sm mt-1">Reputation Intelligence — in configurazione</p>
            </div>
            <Link
              href="/dashboard/ocio/settings"
              className="flex items-center gap-2 px-4 py-2 border border-[#E0E0E0] rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors"
            >
              <Settings size={16} />
              Impostazioni
            </Link>
          </div>

          <div className="bg-white border border-[#E8E8E8] rounded-xl p-8 text-center">
            <Eye size={48} className="text-gray-300 mx-auto mb-4" />
            <h2 className="text-lg font-semibold text-gray-900 mb-2">Dashboard in arrivo</h2>
            <p className="text-sm text-gray-500 mb-4">
              Qui vedrai tutte le recensioni analizzate dall&apos;AI con sentiment, urgenza e risposte suggerite.
            </p>
            <Link
              href="/dashboard/ocio/settings"
              className="inline-flex items-center gap-2 bg-[#111111] text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-[#333333] transition-colors"
            >
              <Settings size={15} />
              Configura URL Google Maps
            </Link>
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    grep -n "ocio" /c/Users/Zanni/fidelityapp/app/dashboard/settings/page.tsx && grep -n "isBusiness\|UpgradePrompt" /c/Users/Zanni/fidelityapp/app/dashboard/ocio/page.tsx
  </verify>
  <done>
    - settings/page.tsx ha link a /dashboard/ocio/settings nella sezione Integrazioni
    - app/dashboard/ocio/page.tsx esiste con gating isBusiness
    - Merchant non-business che visita /dashboard/ocio vede UpgradePrompt BUSINESS
    - Merchant BUSINESS che visita /dashboard/ocio vede placeholder con link a settings
  </done>
</task>

</tasks>

<verification>
1. npm run build — nessun errore TypeScript
2. Aprire dashboard come merchant FREE → sidebar NON mostra sezione OCIO
3. Aprire dashboard come merchant BUSINESS → sidebar mostra sezione "OCIO" con voce Eye "OCIO"
4. /dashboard/settings → sezione Integrazioni ha terzo link "OCIO — Reputation Intelligence"
5. /dashboard/ocio come FREE/PRO → UpgradePrompt BUSINESS
6. /dashboard/ocio come BUSINESS → placeholder con link a Impostazioni
</verification>

<success_criteria>
- Sidebar mostra voce OCIO (icona Eye, label "OCIO", link /dashboard/ocio) solo per piano business
- /dashboard/settings ha card link per /dashboard/ocio/settings nella sezione Integrazioni
- /dashboard/ocio gata BUSINESS: UpgradePrompt per free/pro, placeholder per business
- Nessuna regressione nelle voci esistenti della sidebar (WhatsApp, Chatbot AI, ecc.)
- npm run build senza errori
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/13-ocio-foundation/13-03-SUMMARY.md` con:
- Sidebar.tsx: aggiunto isBusiness state + sezione OCIO condizionale (Eye icon, label "OCIO", href /dashboard/ocio)
- settings/page.tsx: aggiunto link card OCIO nella sezione Integrazioni
- app/dashboard/ocio/page.tsx: stub con gating isBusiness e UpgradePrompt BUSINESS
- Nota: Phase 16 sostituira il stub con la dashboard recensioni completa
</output>
