---
phase: 13-ocio-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - MANUAL-ACTIONS.md
  - lib/types.ts
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - OCIO-01
  - SET-01

must_haves:
  truths:
    - "MANUAL-ACTIONS.md contiene le istruzioni SQL per creare ocio_config e ocio_reviews con tutti gli indici"
    - "lib/types.ts esporta OcioConfig e OcioReview con tutti i campi del DB schema"
    - "apify-client e presente in package.json dependencies"
  artifacts:
    - path: "MANUAL-ACTIONS.md"
      provides: "SQL migrations ocio_config + ocio_reviews per Supabase"
      contains: "CREATE TABLE ocio_config"
    - path: "lib/types.ts"
      provides: "Tipi TypeScript per OCIO"
      exports: ["OcioConfig", "OcioReview"]
  key_links:
    - from: "lib/types.ts OcioConfig"
      to: "ocio_config table"
      via: "campi identici a schema DB"
      pattern: "google_maps_url.*module_reviews.*module_alerts"
    - from: "lib/types.ts OcioReview"
      to: "ocio_reviews table"
      via: "campi identici a schema DB"
      pattern: "ai_sentiment.*ai_urgency.*ai_suggested_reply"

user_setup:
  - service: supabase
    why: "Creazione tabelle OCIO nel DB"
    dashboard_config:
      - task: "Eseguire SQL OCIO nel SQL Editor di Supabase"
        location: "Supabase Dashboard > SQL Editor > New query"
---

<objective>
Pone le basi infrastrutturali del modulo OCIO: documentazione SQL per le due tabelle principali (con istruzioni per il merchant/dev a eseguire manualmente in Supabase), tipi TypeScript completi per OcioConfig e OcioReview, e installazione di apify-client che servira al pipeline di scraping in Phase 14.

Purpose: I piani successivi (13-02, 13-03) dipendono dai tipi OcioConfig/OcioReview. Il DB deve esistere prima di poter testare le API OCIO.
Output: MANUAL-ACTIONS.md aggiornato con SQL OCIO, lib/types.ts con tipi OCIO, apify-client in package.json.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ocio-foundation/13-CONTEXT.md

<interfaces>
<!-- Pattern Supabase service role (API routes) -->
```typescript
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
)
```

<!-- Schema DB ocio_config (da 13-CONTEXT.md) -->
```sql
CREATE TABLE IF NOT EXISTS ocio_config (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  merchant_id UUID REFERENCES merchants(id) ON DELETE CASCADE UNIQUE,
  google_maps_url TEXT,
  google_place_id TEXT,
  place_name TEXT,
  google_access_token TEXT,
  google_refresh_token TEXT,
  google_token_expires_at TIMESTAMPTZ,
  google_account_connected BOOLEAN DEFAULT false,
  business_description TEXT,
  reply_tone TEXT DEFAULT 'professional',
  module_reviews BOOLEAN DEFAULT true,
  module_alerts BOOLEAN DEFAULT true,
  module_social BOOLEAN DEFAULT false,
  module_competitor BOOLEAN DEFAULT false,
  module_price BOOLEAN DEFAULT false,
  module_reports BOOLEAN DEFAULT false,
  alert_whatsapp_number TEXT,
  alert_min_rating INT DEFAULT 3,
  last_scrape_at TIMESTAMPTZ,
  trigger_schedule_id TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

<!-- Schema DB ocio_reviews (da 13-CONTEXT.md) -->
```sql
CREATE TABLE IF NOT EXISTS ocio_reviews (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  merchant_id UUID REFERENCES merchants(id) ON DELETE CASCADE,
  review_id TEXT NOT NULL,
  author_name TEXT,
  author_url TEXT,
  rating INT,
  text TEXT,
  published_at TIMESTAMPTZ,
  owner_reply TEXT,
  owner_reply_at TIMESTAMPTZ,
  review_url TEXT,
  place_id TEXT,
  ai_sentiment TEXT,
  ai_score INT,
  ai_themes TEXT[],
  ai_urgency TEXT,
  ai_category TEXT,
  ai_summary TEXT,
  ai_is_fake BOOLEAN DEFAULT false,
  ai_fake_reason TEXT,
  ai_suggested_reply TEXT,
  ai_analyzed_at TIMESTAMPTZ,
  reply_status TEXT DEFAULT 'pending',
  replied_at TIMESTAMPTZ,
  alert_sent BOOLEAN DEFAULT false,
  alert_sent_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(merchant_id, review_id)
);
CREATE INDEX IF NOT EXISTS idx_ocio_reviews_merchant ON ocio_reviews(merchant_id);
CREATE INDEX IF NOT EXISTS idx_ocio_reviews_rating ON ocio_reviews(rating);
CREATE INDEX IF NOT EXISTS idx_ocio_reviews_published ON ocio_reviews(published_at DESC);
CREATE INDEX IF NOT EXISTS idx_ocio_reviews_pending ON ocio_reviews(merchant_id) WHERE reply_status = 'pending';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere SQL OCIO a MANUAL-ACTIONS.md</name>
  <files>MANUAL-ACTIONS.md</files>
  <action>
    Appendere una nuova sezione "## 6. Modulo OCIO — tabelle ocio_config e ocio_reviews" a MANUAL-ACTIONS.md con il SQL completo per creare entrambe le tabelle e i 4 indici su ocio_reviews. Includere anche le tabelle stub ausiliarie (ocio_competitor_data, ocio_social_data, ocio_monthly_reports, ocio_alerts_log) come CREATE TABLE IF NOT EXISTS minimali (id UUID PK + merchant_id FK + created_at), con nota che vengono popolate nelle fasi future.

    Il SQL deve usare IF NOT EXISTS ovunque per idempotency. Struttura della sezione:

    ```
    ## 6. Modulo OCIO — tabelle ocio_config e ocio_reviews

    Richiesto da: Phase 13 — OCIO Foundation

    ### Tabella ocio_config
    [SQL completo con tutti i campi dalle interfaces di questo piano]

    ### Tabella ocio_reviews + indici
    [SQL completo con tutti i campi + i 4 indici]

    ### Tabelle stub per fasi future
    [CREATE TABLE IF NOT EXISTS per ocio_competitor_data, ocio_social_data, ocio_monthly_reports, ocio_alerts_log]
    ```

    Copiare esattamente il SQL dalle sezioni <interfaces> di questo piano. Non modificare le sezioni 1-5 esistenti di MANUAL-ACTIONS.md.
  </action>
  <verify>grep -n "ocio_config\|ocio_reviews\|ocio_alerts_log" /c/Users/Zanni/fidelityapp/MANUAL-ACTIONS.md</verify>
  <done>MANUAL-ACTIONS.md contiene sezione 6 con SQL per tutte le tabelle OCIO (ocio_config, ocio_reviews con 4 indici, piu 4 stub tables)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Eseguire SQL OCIO in Supabase</name>
  <what-built>SQL OCIO documentato in MANUAL-ACTIONS.md (sezione 6)</what-built>
  <how-to-verify>
    1. Apri Supabase Dashboard > SQL Editor > New query
    2. Copia ed esegui il SQL della sezione 6 di MANUAL-ACTIONS.md (tutte le tabelle OCIO)
    3. Verifica: Table Editor mostra le nuove tabelle: ocio_config, ocio_reviews, ocio_competitor_data, ocio_social_data, ocio_monthly_reports, ocio_alerts_log
    4. Conferma che non ci sono errori SQL

    NOTA: Se le tabelle esistono gia (da test precedenti), il SQL IF NOT EXISTS le skippa senza errore — questo e corretto.
  </how-to-verify>
  <resume-signal>Digita "fatto" quando le tabelle esistono in Supabase, oppure descrivi l'errore SQL</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aggiungere tipi OcioConfig e OcioReview a lib/types.ts</name>
  <files>lib/types.ts</files>
  <action>
    Appendere alla fine di lib/types.ts una nuova sezione OCIO. NON modificare i tipi esistenti.

    ```typescript
    // =============================================
    // OCIO — REPUTATION INTELLIGENCE
    // =============================================

    export type OcioConfig = {
      id: string
      merchant_id: string
      google_maps_url: string | null
      google_place_id: string | null
      place_name: string | null
      google_account_connected: boolean
      business_description: string | null
      reply_tone: 'professional' | 'warm' | 'formal'
      module_reviews: boolean
      module_alerts: boolean
      module_social: boolean
      module_competitor: boolean
      module_price: boolean
      module_reports: boolean
      alert_whatsapp_number: string | null
      alert_min_rating: number
      last_scrape_at: string | null
      trigger_schedule_id: string | null
      created_at: string
      updated_at: string
    }

    export type OcioReview = {
      id: string
      merchant_id: string
      review_id: string
      author_name: string | null
      author_url: string | null
      rating: number | null
      text: string | null
      published_at: string | null
      owner_reply: string | null
      owner_reply_at: string | null
      review_url: string | null
      place_id: string | null
      ai_sentiment: 'positive' | 'neutral' | 'negative' | null
      ai_score: number | null
      ai_themes: string[] | null
      ai_urgency: 'low' | 'medium' | 'high' | 'critical' | null
      ai_category: string | null
      ai_summary: string | null
      ai_is_fake: boolean
      ai_fake_reason: string | null
      ai_suggested_reply: string | null
      ai_analyzed_at: string | null
      reply_status: 'pending' | 'replied' | 'ignored'
      replied_at: string | null
      alert_sent: boolean
      alert_sent_at: string | null
      created_at: string
    }
    ```
  </action>
  <verify>grep -n "OcioConfig\|OcioReview" /c/Users/Zanni/fidelityapp/lib/types.ts</verify>
  <done>lib/types.ts esporta OcioConfig e OcioReview con tutti i campi corrispondenti allo schema DB</done>
</task>

<task type="auto">
  <name>Task 3: Installare apify-client</name>
  <files>package.json, package-lock.json</files>
  <action>
    Eseguire dal working directory /c/Users/Zanni/fidelityapp:
    ```
    npm install apify-client
    ```

    Questo installa il pacchetto che servira a Phase 14 per chiamare l'actor compass/google-maps-reviews-scraper.

    Nota: @trigger.dev/sdk e gia in package.json (v4.4.1) e trigger.config.ts esiste gia con project ID proj_zvyvldbkgijrsvkohrfs — nessuna azione Trigger.dev necessaria in questa fase.

    Verificare che apify-client compaia in package.json dependencies dopo l'installazione.
  </action>
  <verify>grep "apify-client" /c/Users/Zanni/fidelityapp/package.json</verify>
  <done>apify-client compare nelle dependencies di package.json</done>
</task>

</tasks>

<verification>
1. grep "ocio_config\|ocio_reviews" /c/Users/Zanni/fidelityapp/MANUAL-ACTIONS.md — trova le righe SQL nella sezione 6
2. grep "OcioConfig\|OcioReview" /c/Users/Zanni/fidelityapp/lib/types.ts — trova le definizioni di tipo
3. grep "apify-client" /c/Users/Zanni/fidelityapp/package.json — trova la dipendenza
4. Checkpoint umano confermato: tabelle ocio_config e ocio_reviews visibili in Supabase Table Editor
</verification>

<success_criteria>
- MANUAL-ACTIONS.md ha sezione 6 con SQL completo per 6 tabelle OCIO (con IF NOT EXISTS)
- lib/types.ts esporta OcioConfig (20 campi) e OcioReview (25 campi) corrispondenti allo schema DB
- apify-client e in package.json dependencies
- Tabelle ocio_config e ocio_reviews esistono in Supabase (confermato dal checkpoint)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/13-ocio-foundation/13-01-SUMMARY.md` con:
- Tabelle DB create in Supabase: ocio_config, ocio_reviews (con 4 indici), ocio_competitor_data, ocio_social_data, ocio_monthly_reports, ocio_alerts_log
- Tipi TypeScript aggiunti a lib/types.ts: OcioConfig, OcioReview
- apify-client installato (per Phase 14)
- Nota: @trigger.dev/sdk gia presente v4.4.1, trigger.config.ts gia configurato con project proj_zvyvldbkgijrsvkohrfs
</output>
