---
phase: 02-merchant-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/page.tsx
  - app/dashboard/programs/page.tsx
  - app/dashboard/programs/[id]/page.tsx
  - app/dashboard/programs/new/page.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Merchant apre la dashboard da iPhone 375px e tutte le pagine sono leggibili senza zoom o scroll orizzontale"
    - "Tutti i bottoni, card e form della dashboard hanno lo stesso stile visivo (rounded-2xl card, rounded-xl bottoni, indigo-600 primario)"
    - "Il pulsante Scanner e il menu di navigazione sono raggiungibili con un solo tocco da qualsiasi pagina dashboard"
  artifacts:
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard principale mobile-responsive"
      contains: "px-4"
    - path: "app/dashboard/programs/page.tsx"
      provides: "Lista programmi mobile-responsive"
      contains: "grid-cols-1"
    - path: "app/dashboard/programs/[id]/page.tsx"
      provides: "Dettaglio programma mobile-responsive"
      contains: "px-4"
    - path: "app/dashboard/programs/new/page.tsx"
      provides: "Creazione programma mobile-responsive"
      contains: "px-4"
  key_links:
    - from: "app/dashboard/page.tsx header"
      to: "/stamp"
      via: "Link Scanner button visible and tappable on mobile"
      pattern: "href=\"/stamp\""
    - from: "app/dashboard/programs/page.tsx"
      to: "grid layout"
      via: "grid-cols-1 on mobile, grid-cols-2 on md"
      pattern: "grid-cols-1 md:grid-cols-2"
---

<objective>
Apply mobile-first Tailwind fixes across all four core dashboard pages so a merchant can use the dashboard from an iPhone 375px screen without horizontal scroll, zoom, or layout breakage. Standardize the design language (border-radius, spacing, button padding, shadows) to a single consistent set of values.

Purpose: The dashboard is the merchant's daily tool. If it doesn't work on a phone, the product doesn't work. This plan makes the existing pages responsive — no new features, no new pages.
Output: Four modified .tsx files with consistent, mobile-first Tailwind classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/dashboard/page.tsx
@app/dashboard/programs/page.tsx
@app/dashboard/programs/[id]/page.tsx
@app/dashboard/programs/new/page.tsx
</context>

<interfaces>
<!-- Design system tokens decided in CONTEXT.md — apply these, nothing else -->

Design tokens to enforce:
- Card border-radius: `rounded-2xl` (replace any rounded-xl or rounded-3xl on card containers)
- Input/button border-radius: `rounded-xl` (replace any rounded-lg or rounded-2xl on inputs and secondary buttons)
- Large CTA buttons: `rounded-xl py-4 font-bold` (consistent across all forms)
- Primary color: `indigo-600` (already standard — do not add new colors)
- Card shadow: `shadow-sm` (for dashboard cards — replace shadow-md, shadow-lg on card containers)
- Scanner overlay shadow: `shadow-xl` (for modal/overlay cards — keep as is in stamp/page.tsx which is NOT touched here)
- Container padding mobile: `px-4` (replace px-6 on mobile breakpoints, keep px-6 md:px-8 on desktop)
- Max-width: `max-w-4xl` for single-column content, `max-w-6xl` for grids (replace max-w-7xl on dashboard/page.tsx header which causes overflow on mobile)

Mobile grid fixes:
- Stats grid: already `grid-cols-2 md:grid-cols-3 lg:grid-cols-6` — KEEP, only fix container padding
- Quick actions: change `md:grid-cols-4` to `grid-cols-2 md:grid-cols-4` (stack to 2 on mobile)
- Programs grid: add `grid-cols-1` as base before `md:grid-cols-2 lg:grid-cols-3`
- Header flex row: add `flex-wrap gap-2` so header buttons don't overflow on narrow screens

Navigation fix on dashboard/page.tsx:
- Header Scanner link: already present — ensure it has at minimum `px-3 py-2` touch target, add `text-sm` for small screens
- Add `flex-shrink-0` to header button group so buttons don't get squeezed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard/page.tsx — mobile container + quick actions grid</name>
  <files>app/dashboard/page.tsx</files>
  <action>
    Apply these specific changes to app/dashboard/page.tsx:

    1. Header `max-w-7xl` → `max-w-6xl` and add `flex-wrap gap-y-2` to the inner flex div so header buttons wrap on narrow screens. Add `flex-shrink-0` to the button group div.

    2. Header Scanner link: change `px-4 py-2 rounded-lg` → `px-3 py-2 rounded-xl text-sm font-medium`. Settings link: change `px-4 py-2 rounded-lg` → `p-2 rounded-xl`.

    3. Main `p-6 max-w-7xl` → `px-4 py-6 max-w-6xl` (narrower padding on mobile, keep centering).

    4. Quick actions grid: change `grid md:grid-cols-4` → `grid grid-cols-2 md:grid-cols-4`. Each quick action Link card: change `rounded-xl` → `rounded-2xl`. Keep padding `p-6` as is.

    5. Stats grid cards: change `rounded-xl` → `rounded-2xl`. No other changes to stats grid (layout is already good).

    6. Activity and Top Customers cards: change `rounded-xl` → `rounded-2xl`. Keep `shadow-sm`.

    7. Empty state alert at bottom: change `rounded-xl` → `rounded-2xl`. The CTA Link inside: change `rounded-lg` → `rounded-xl`.

    Do NOT change any data-loading logic, state management, or color values.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>dashboard/page.tsx passes TypeScript check, header buttons wrap correctly, quick actions show 2 columns on mobile, all cards use rounded-2xl</done>
</task>

<task type="auto">
  <name>Task 2: Fix programs pages — mobile layout on list + detail + new</name>
  <files>app/dashboard/programs/page.tsx, app/dashboard/programs/[id]/page.tsx, app/dashboard/programs/new/page.tsx</files>
  <action>
    Apply mobile fixes to all three program pages.

    **app/dashboard/programs/page.tsx:**
    1. Header `px-6` → `px-4` on mobile: change `px-6 py-4` → `px-4 py-4`. Header inner div: add `flex-wrap gap-2`.
    2. "Nuovo Programma" link in header: change `rounded-lg` → `rounded-xl`, add `text-sm`.
    3. Main `p-6 max-w-6xl` → `px-4 py-6 max-w-6xl`.
    4. Programs grid: change `grid md:grid-cols-2 lg:grid-cols-3` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`. Mobile shows 1 column.
    5. Program card container Link: change `rounded-xl` → `rounded-2xl`.
    6. Add new card (dashed border): change `rounded-xl` → `rounded-2xl`.

    **app/dashboard/programs/[id]/page.tsx:**
    1. Header: change `px-6` → `px-4`, add `flex-wrap gap-2` to header inner flex.
    2. Main container: change `p-6` → `px-4 py-6`, reduce max-w if wider than max-w-6xl.
    3. Any card containers using `rounded-xl` → `rounded-2xl`.
    4. Any button/input `rounded-lg` → `rounded-xl`.
    5. Modal overlays: keep `rounded-2xl` or `rounded-3xl` as they are (modal UX is out of scope for this plan).

    **app/dashboard/programs/new/page.tsx:**
    1. Main container: change `p-6` or outer padding → `px-4 py-6`.
    2. Program type selector cards: change `rounded-xl` → `rounded-2xl`. These are the big type-picker cards in step 1.
    3. Form inputs: ensure `rounded-xl` (not rounded-lg). Check all `<input>` and `<textarea>` elements.
    4. Submit/next buttons: ensure `py-4 rounded-xl font-bold` pattern.
    5. If any section uses `px-6` as the outermost padding at mobile sizes, change to `px-4`.

    Do NOT change any form submission logic, validation, step navigation, or state.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three program pages pass TypeScript check. Programs list shows 1 column on mobile. All card containers use rounded-2xl. All form inputs use rounded-xl. Container padding is px-4 at mobile.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` returns 0 errors
- Open http://localhost:3000/dashboard in browser devtools at 375px width — no horizontal scrollbar visible
- Open http://localhost:3000/dashboard/programs at 375px — programs show 1 column, no overflow
- Open http://localhost:3000/dashboard/programs/new at 375px — form fields not clipped
- All card components visually use rounded-2xl (more rounded than before if changed from rounded-xl)
</verification>

<success_criteria>
1. All four dashboard pages pass TypeScript compile with no new errors
2. At 375px viewport width (iPhone SE): no horizontal scroll on any of the four pages
3. Design is consistent: card containers are rounded-2xl, buttons/inputs are rounded-xl, primary color is indigo-600
4. Quick action grid on dashboard: 2 columns on mobile, 4 columns on md+
5. Programs list grid: 1 column on mobile, 2 columns on md+
</success_criteria>

<output>
After completion, create `.planning/phases/02-merchant-ux/02-01-SUMMARY.md` following the summary template.
</output>
