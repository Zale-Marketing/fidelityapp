---
phase: 05-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04

must_haves:
  truths:
    - "Visitatore legge sopra the fold: headline audience-direct (bar, ristoranti, negozi), sub-headline di supporto, e CTA 'Inizia Gratis'"
    - "Visitatore vede la social proof bar con 3 stat statiche: 50+ attività attive, 1.000+ carte emesse, 5 tipi di programma"
    - "Visitatore vede la sezione 'Come funziona' con 3 card numerate (cerchi indigo 1/2/3) che illustrano il flusso"
    - "Tutti i link CTA puntano a /register — nessun link esterno, nessun form inline"
    - "Merchant appena registrato (via /register esistente) viene reindirizzato a /onboarding — già implementato, invariato"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page pubblica completa — navbar, hero, social proof, 3-step, footer"
      min_lines: 150
  key_links:
    - from: "app/page.tsx CTA 'Inizia Gratis'"
      to: "/register"
      via: "Link href='/register'"
      pattern: "href.*register"
    - from: "app/register/page.tsx (invariata)"
      to: "/onboarding"
      via: "router.push('/onboarding') — già implementato"
      pattern: "router.push.*onboarding"
---

<objective>
Sostituire interamente `app/page.tsx` con una landing page professionale a sfondo bianco che converte visitatori esterni in merchant registrati.

Purpose: Un visitatore (bar, ristorante, negozio, palestra) deve capire il valore di FidelityApp sopra the fold e completare la registrazione senza assistenza di Alessandro.

Output:
- `app/page.tsx` riscritto con: navbar, sezione hero (headline + mockup telefono CSS), social proof bar, sezione "Come funziona" (3 step), footer
- Stile: bg-white, accenti indigo-600, Tailwind puro, nessuna immagine esterna
- `/register` e `/onboarding` invariati — la landing si aggancia al flusso già esistente
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-landing-page/05-CONTEXT.md
</context>

<interfaces>
<!-- Existing patterns from codebase — executor uses these directly. -->

From app/page.tsx (current — will be fully replaced):
- Route: app/page.tsx → public route `/`
- Imports: `import Link from 'next/link'` (no 'use client' needed — purely static)

From app/register/page.tsx (unchanged):
- Route: `/register`
- On success: `router.push('/onboarding')` — LAND-04 already satisfied

Established Tailwind tokens (from CONTEXT.md):
- CTA button: `bg-indigo-600 text-white px-6 py-3 rounded-xl font-semibold hover:bg-indigo-700`
- Card: `bg-white rounded-xl shadow-lg p-6`
- Accent text: `text-indigo-600`
- Section padding: `px-4 py-6` mobile, `px-6 py-16` desktop
- Card/button radius: `rounded-xl`, `rounded-2xl` for larger cards
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app/page.tsx — landing page completa</name>
  <files>app/page.tsx</files>
  <action>
Sostituire INTERAMENTE il contenuto di `app/page.tsx` con la seguente struttura (pagina statica, no 'use client', solo `import Link from 'next/link'`):

**1. Navbar** — sticky top, bg-white, border-b border-gray-100, shadow-sm
  - Sinistra: testo "FidelityApp" font-bold text-indigo-600 text-xl
  - Destra: link "Accedi" → /login (text-gray-600 hover:text-gray-900) + pulsante "Inizia Gratis" → /register (bg-indigo-600 text-white px-4 py-2 rounded-xl font-semibold hover:bg-indigo-700)

**2. Sezione Hero** — bg-white, py-16 lg:py-24, max-w-6xl mx-auto px-4
  - Layout: `flex flex-col lg:flex-row items-center gap-12`
  - Sinistra (testo, flex-1):
    - Badge pill sopra headline: `bg-indigo-50 text-indigo-600 text-sm font-medium px-3 py-1 rounded-full inline-block mb-4` — testo: "Carta fedeltà digitale"
    - Headline h1: `text-4xl lg:text-5xl font-bold text-gray-900 leading-tight mb-4` — testo esatto: "Bar, ristoranti e negozi: la carta fedeltà digitale per i tuoi clienti"
    - Sub-headline p: `text-xl text-gray-600 mb-8 leading-relaxed` — testo: "Crei il programma in 5 minuti. I clienti salvano la carta su Google Wallet. Il cassiere timbra con la fotocamera — nessuna app da scaricare."
    - CTA primaria: Link href="/register" `bg-indigo-600 text-white px-8 py-4 rounded-xl text-lg font-bold hover:bg-indigo-700 shadow-lg inline-block` — testo: "Inizia Gratis →"
    - Testo sotto CTA: `text-sm text-gray-500 mt-3` — "Fino a 5 programmi gratis. Nessuna carta di credito."
  - Destra (mockup telefono CSS, flex-shrink-0):
    - Contenitore telefono: `w-64 lg:w-72 mx-auto` con sfondo rounded-3xl border-4 border-gray-800 shadow-2xl overflow-hidden bg-gray-800`
    - Intestazione telefono: piccola barra grigio scuro con cerchio speaker (rounded-full bg-gray-600 w-16 h-1.5 mx-auto)
    - Carta Google Wallet simulata dentro il telefono: `bg-indigo-600 rounded-2xl mx-3 my-2 p-4`
      - Riga sopra: `text-white text-xs font-medium opacity-70` → "Google Wallet"
      - Nome attività: `text-white font-bold text-lg mt-2` → "Bar Roma"
      - Tipo programma: `text-white text-sm opacity-80` → "Carta Fedeltà · Bollini"
      - Contatore bollini: riga flex con cerchi — mostrare 5 cerchi pieni (bg-white rounded-full w-6 h-6) e 5 vuoti (border-2 border-white/50 rounded-full w-6 h-6) in una griglia flex-wrap gap-1 mt-3
      - Testo sotto: `text-white text-sm mt-2 font-semibold` → "5 / 10 bollini"
      - Testo premio: `text-white text-xs opacity-70 mt-1` → "Ancora 5 per un caffè gratis"
    - Footer telefono: `bg-gray-100 py-2 text-center text-xs text-gray-400` → "powered by FidelityApp"

**3. Social Proof Bar** — bg-gray-50 border-y border-gray-100 py-8
  - max-w-4xl mx-auto px-4
  - Layout: `flex flex-col sm:flex-row items-center justify-center gap-8 sm:gap-16`
  - 3 stat item: ciascuno `text-center`
    - Numero: `text-2xl font-bold text-indigo-600`
    - Label: `text-sm text-gray-500 mt-1`
  - Stat 1: "50+" / "attività attive"
  - Stat 2: "1.000+" / "carte emesse"
  - Stat 3: "5" / "tipi di programma"
  - Separatori tra stat (solo su sm+): `hidden sm:block w-px h-10 bg-gray-200`

**4. Sezione "Come funziona"** — bg-white py-20 px-4
  - Titolo sezione: `text-3xl font-bold text-gray-900 text-center mb-4` → "Come funziona"
  - Sottotitolo: `text-gray-500 text-center mb-12 text-lg` → "Tre passi. Zero app. Zero attrito."
  - max-w-5xl mx-auto
  - Grid: `grid grid-cols-1 md:grid-cols-3 gap-8`
  - 3 card step (bg-white rounded-2xl p-8 shadow-sm border border-gray-100 text-center):
    - Cerchio numerato: `w-12 h-12 bg-indigo-600 rounded-full flex items-center justify-center text-white font-bold text-xl mx-auto mb-6`
    - Titolo step: `text-xl font-bold text-gray-900 mb-3`
    - Descrizione: `text-gray-600 leading-relaxed`
    - Step 1: numero 1 / "Crei il programma" / "Scegli il tipo (bollini, punti, cashback) e personalizzi i colori. Pronto in 5 minuti."
    - Step 2: numero 2 / "Il cliente scansiona il QR" / "Con la fotocamera del telefono, senza app da scaricare. Funziona su qualsiasi smartphone."
    - Step 3: numero 3 / "Google Wallet si aggiorna" / "La carta del cliente si aggiorna in tempo reale ad ogni visita."

**5. CTA finale** — bg-indigo-600 py-20 px-4 text-center
  - Titolo: `text-3xl font-bold text-white mb-4` → "Inizia oggi, è gratis"
  - Sub: `text-indigo-100 text-lg mb-8` → "Crea il tuo primo programma fedeltà in 5 minuti."
  - CTA: Link href="/register" `bg-white text-indigo-600 px-8 py-4 rounded-xl text-lg font-bold hover:bg-gray-100 shadow-lg inline-block` → "Inizia Gratis →"
  - Testo sotto: `text-indigo-200 text-sm mt-4` → "Nessuna carta di credito richiesta."

**6. Footer** — bg-gray-900 py-8 px-4 text-center
  - Testo: `text-gray-400 text-sm` → "© 2026 FidelityApp by Zale Marketing"
  - Link: `text-gray-400 hover:text-white text-sm ml-4` → Link href="/login" → "Accedi"

NOTE: NON usare 'use client'. NON usare `<img>` tag. NON usare emoji. Solo `import Link from 'next/link'` come import. La pagina deve essere un Server Component puro.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "app/page.tsx" | head -10 || echo "No TS errors in page.tsx"</automated>
  </verify>
  <done>
    - `app/page.tsx` è un Server Component puro (no 'use client')
    - Navbar presente con link "Accedi" → /login e "Inizia Gratis" → /register
    - Hero con headline esatta: "Bar, ristoranti e negozi: la carta fedeltà digitale per i tuoi clienti"
    - Mockup telefono CSS con carta indigo, "Bar Roma", "5 / 10 bollini"
    - Social proof bar con 3 stat (50+ attività, 1.000+ carte, 5 tipi)
    - Sezione "Come funziona" con 3 card e cerchi numerati indigo
    - CTA finale e footer presenti
    - `npx tsc --noEmit` non riporta errori in app/page.tsx
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica visiva landing page</name>
  <what-built>Landing page completa in app/page.tsx — navbar, hero con mockup telefono CSS, social proof bar, sezione "Come funziona" con 3 step numerati, CTA finale, footer.</what-built>
  <how-to-verify>
1. Avvia il server di sviluppo: `npm run dev`
2. Apri http://localhost:3000 nel browser
3. Verifica sopra the fold (senza scrollare):
   - Navbar con "FidelityApp" in indigo a sinistra, "Accedi" e "Inizia Gratis" a destra
   - Headline visibile: "Bar, ristoranti e negozi: la carta fedeltà digitale per i tuoi clienti"
   - Mockup telefono con carta indigo "Bar Roma" e bollini visibili a destra
4. Scrolla verso il basso e verifica:
   - Striscia social proof: "50+ attività attive", "1.000+ carte emesse", "5 tipi di programma"
   - Sezione "Come funziona" con 3 card — numeri 1, 2, 3 in cerchi indigo
   - Sezione CTA finale su sfondo indigo
   - Footer grigio scuro con copyright
5. Clicca "Inizia Gratis" — deve aprire /register (pagina di registrazione già esistente)
6. Verifica mobile: ridimensiona a 390px — tutto deve stackarsi verticalmente senza overflow orizzontale
  </how-to-verify>
  <resume-signal>Scrivi "approvato" se la pagina è visivamente corretta, oppure descrivi i problemi da correggere</resume-signal>
</task>

</tasks>

<verification>
- `app/page.tsx` è un Server Component (no 'use client') — verificabile con grep
- Tutti i link CTA puntano a /register — `grep "href.*register" app/page.tsx`
- Nessun emoji nel codice — `grep -P "[\x{1F300}-\x{1F9FF}]" app/page.tsx`
- Nessuna immagine esterna (`<img>` tag) — solo CSS puro
- TypeScript compila senza errori: `npx tsc --noEmit`
</verification>

<success_criteria>
- Visitatore vede sopra the fold: headline audience-direct, mockup telefono, CTA "Inizia Gratis →"
- Visitatore vede social proof bar con 3 stat statiche hard-coded
- Visitatore vede sezione "Come funziona" con 3 step numerati (cerchi indigo 1/2/3)
- Clic su "Inizia Gratis" porta a /register — già funzionante
- /register → /onboarding già implementato (invariato) — LAND-04 soddisfatto
- Pagina funziona su mobile (stacking, no overflow orizzontale)
- Nessun errore TypeScript, nessuna immagine esterna, nessun emoji
</success_criteria>

<output>
After completion, create `.planning/phases/05-landing-page/05-01-SUMMARY.md`
</output>
