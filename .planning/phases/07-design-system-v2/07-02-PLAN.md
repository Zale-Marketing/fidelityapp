---
phase: 07-design-system-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/dashboard/page.tsx
  - app/dashboard/programs/page.tsx
  - app/dashboard/notifications/page.tsx
  - app/dashboard/settings/page.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-05
  - DESIGN-06
  - DESIGN-08
  - DESIGN-11

must_haves:
  truths:
    - "Dashboard home non contiene nessuna emoji — tutte sostituite con icone Lucide o rimosse"
    - "Programs list non contiene emoji — tipo programma mostrato con icona Lucide"
    - "Notifications page non contiene emoji"
    - "Settings page non contiene emoji"
    - "Tutte le 4 pagine usano bottoni #111111 e sfondo #F5F5F5 (dal layout)"
    - "Nessuna pagina ha un proprio wrapper min-h-screen o bg-gray-* che sovrascrive il layout"
  artifacts:
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard home con MetricCard e zero emoji"
    - path: "app/dashboard/programs/page.tsx"
      provides: "Lista programmi con icone Lucide per tipo programma"
    - path: "app/dashboard/notifications/page.tsx"
      provides: "Notifiche senza emoji"
    - path: "app/dashboard/settings/page.tsx"
      provides: "Impostazioni senza emoji"
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "components/ui/MetricCard.tsx"
      via: "import MetricCard"
      pattern: "import MetricCard"
    - from: "app/dashboard/programs/page.tsx"
      to: "components/ui/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "import EmptyState"
---

<objective>
Applicare il design system alle 4 pagine dashboard più semplici: home, programs list, notifications, settings. Rimuovere tutte le emoji, sostituire colori indigo con token neutrali, eliminare wrapper standalone che conflittano con il layout.

Purpose: Queste 4 pagine hanno struttura relativamente semplice (224-500 linee) e nessuna dipendenza tra loro — eseguibili in un solo piano parallelo a 07-03 e 07-04.
Output: 4 pagine aggiornate con zero emoji, bottoni #111111, font Inter, sfondo ereditato dal layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-design-system-v2/07-RESEARCH.md
@.planning/phases/07-design-system-v2/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Componenti disponibili dopo 07-01 -->

```typescript
// components/ui/MetricCard.tsx
import MetricCard from '@/components/ui/MetricCard'
// Props: label, value, icon?, valueClassName?, trend?

// components/ui/StatusBadge.tsx
import StatusBadge from '@/components/ui/StatusBadge'
// Props: variant ('active'|'inactive'|'expired'|'pending'), label?

// components/ui/EmptyState.tsx
import EmptyState from '@/components/ui/EmptyState'
// Props: icon (LucideIcon), title, description, actionLabel?, actionHref?

// Lucide icons disponibili (import da 'lucide-react'):
// Home, Target, Users, Bell, BarChart2, CreditCard, Settings,
// Plus, Zap, Trophy, Tag, Star, Coins, Crown, RefreshCw, Stamp,
// Mail, Phone, Trash2, Check, X, Rocket, Search, TrendingUp
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aggiorna dashboard home (app/dashboard/page.tsx)</name>
  <files>app/dashboard/page.tsx</files>
  <action>
Leggi il file esistente prima di modificarlo. Poi applica queste trasformazioni:

1. RIMUOVI il wrapper esterno con `min-h-screen` e/o `bg-gray-*` — il layout dashboard gestisce sfondo e altezza minima.

2. STRUTTURA PAGINA: Wrap tutto in `<div className="px-6 py-6">`.

3. PAGE HEADER: Sostituisci qualsiasi header esistente con:
   ```tsx
   <div className="flex items-center justify-between mb-6">
     <div>
       <h1 className="text-2xl font-semibold text-gray-900">Dashboard</h1>
       <p className="text-sm text-gray-500 mt-1">Benvenuto, {merchantName || 'merchant'}</p>
     </div>
   </div>
   ```
   Rimuovi emoji di saluto (👋) — usa solo il testo.

4. METRICHE: Sostituisci le card metriche attuali con `<MetricCard>` componente. Importa da `@/components/ui/MetricCard`. Per le icone nelle metriche usa icone Lucide (es. `<Users size={20} />` per clienti, `<Target size={20} />` per programmi, `<Zap size={20} />` per transazioni).

5. COLORI PRIMARI: Sostituisci TUTTI `bg-indigo-600` → `bg-[#111111]`, `hover:bg-indigo-700` → `hover:bg-[#333333]`, `text-indigo-*` → `text-gray-900` o `text-[#111111]`.

6. EMPTY STATES: Se c'è uno stato vuoto con emoji, sostituisci con `<EmptyState>` usando icona Lucide appropriata.

7. EMOJI DA RIMUOVERE: Ogni emoji (👋, ⚡, 🏆, 🎯, 👥, 📢, 📊, 🚀, ecc.) va eliminata o sostituita con icona Lucide. Se un'emoji è solo decorativa (es. prima di un titolo), rimuovila. Se è un'icona funzionale, sostituisci con Lucide.

8. Mantieni TUTTA la logica esistente (fetch dati, auth check, redirect, loading states) — cambia solo la UI.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && grep -c "emoji\|🎯\|👥\|📊\|🚀\|⚡\|🏆\|👋\|📢" app/dashboard/page.tsx || echo "0 emoji found"</automated>
  </verify>
  <done>app/dashboard/page.tsx: zero emoji, usa MetricCard, bottoni #111111, nessun wrapper min-h-screen/bg-gray-* standalone.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiorna programs list + notifications + settings</name>
  <files>app/dashboard/programs/page.tsx, app/dashboard/notifications/page.tsx, app/dashboard/settings/page.tsx</files>
  <action>
Leggi ciascun file prima di modificarlo. Applica le stesse trasformazioni sistematiche a tutti e tre:

**REGOLE GENERALI per ogni file:**

A. RIMUOVI il wrapper esterno con `min-h-screen` e/o `bg-gray-*` standalone.

B. STRUTTURA: Wrap contenuto in `<div className="px-6 py-6">`.

C. PAGE HEADER standardizzato (senza emoji):
   ```tsx
   <div className="flex items-center justify-between mb-6">
     <div>
       <h1 className="text-2xl font-semibold text-gray-900">[Nome Pagina]</h1>
       <p className="text-sm text-gray-500 mt-1">[Sottotitolo]</p>
     </div>
     {/* CTA button se applicabile */}
   </div>
   ```

D. BOTTONI PRIMARI: `bg-[#111111] text-white hover:bg-[#333333] rounded-[8px] px-4 py-2.5 text-sm font-medium transition-colors`. Rimuovi ogni `bg-indigo-*`.

E. BOTTONI SECONDARI: `border border-[#E0E0E0] text-gray-700 hover:bg-[#F5F5F5] rounded-[8px] px-4 py-2.5 text-sm font-medium transition-colors`.

F. TABELLE (se presenti): Header `bg-[#F9F9F9]`, righe `border-b border-[#F0F0F0] last:border-0`, nessun `divide-y` o `hover:bg-gray-50` alternato. Wrapper `bg-white rounded-[12px] border border-[#E8E8E8] overflow-hidden`.

G. INPUTS (se presenti): `border border-[#E0E0E0] rounded-[8px] px-3 py-3 text-sm focus:border-[#111111] focus:outline-none transition-colors`. Rimuovi `focus:ring-*`.

H. EMPTY STATES: Sostituisci con `<EmptyState>` importato da `@/components/ui/EmptyState`.

I. EMOJI: Rimuovi TUTTE le emoji. Per i tipi programma in programs/page.tsx (🎫, ⭐, 💰, 👑, 🔄), usa icone Lucide:
   - stamps → `<Stamp size={16} />`
   - points → `<Star size={16} />`
   - cashback → `<Coins size={16} />`
   - tiers → `<Crown size={16} />`
   - subscription → `<RefreshCw size={16} />`

J. STATUS BADGE: Se ci sono badge attivo/inattivo, usa `<StatusBadge>` da `@/components/ui/StatusBadge`.

K. Mantieni TUTTA la logica di business, fetch, auth, state management — solo la presentazione cambia.

**Per notifications/page.tsx specificatamente:**
- Il tab "Push Notification" mantiene il suo testo (senza emoji 📢)
- I form di invio notifiche usano input e bottoni standardizzati
- Il selettore programma/tag usa le classi input standardizzate

**Per settings/page.tsx specificatamente:**
- Le sezioni impostazioni usano `bg-white border border-[#E8E8E8] rounded-[12px] p-6` per ogni card
- Rimuovi emoji ⚙️ dal titolo
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && for f in app/dashboard/programs/page.tsx app/dashboard/notifications/page.tsx app/dashboard/settings/page.tsx; do count=$(grep -c "🎫\|⭐\|💰\|👑\|🔄\|📢\|⚙️\|✅\|❌" "$f" 2>/dev/null || echo 0); echo "$f: $count emoji"; done</automated>
  </verify>
  <done>Tutte e tre le pagine: zero emoji, bottoni #111111, tabelle con header #F9F9F9, inputs focus:border-[#111111], StatusBadge dove applicabile, EmptyState dove applicabile.</done>
</task>

</tasks>

<verification>
Dopo l'esecuzione:
1. `grep -rn "bg-indigo\|text-indigo\|ring-indigo" app/dashboard/page.tsx app/dashboard/programs/page.tsx app/dashboard/notifications/page.tsx app/dashboard/settings/page.tsx` — deve tornare vuoto
2. `grep -rn "min-h-screen" app/dashboard/page.tsx app/dashboard/programs/page.tsx app/dashboard/notifications/page.tsx app/dashboard/settings/page.tsx` — deve tornare vuoto (il layout gestisce min-h-screen)
3. `npm run build` non mostra errori TypeScript per questi file
</verification>

<success_criteria>
- Nessuna emoji in app/dashboard/page.tsx, programs/page.tsx, notifications/page.tsx, settings/page.tsx
- Tutti gli `bg-indigo-*` sostituiti con `bg-[#111111]`
- Tutti `focus:ring-*` sostituiti con `focus:border-[#111111] focus:outline-none`
- MetricCard usato nella dashboard home
- EmptyState usato dove ci sono liste vuote
- StatusBadge usato per badge attivo/inattivo
- Nessun wrapper `min-h-screen bg-gray-*` standalone nelle 4 pagine
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-v2/07-02-SUMMARY.md`
</output>
