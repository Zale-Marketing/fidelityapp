---
phase: 07-design-system-v2
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/dashboard/programs/new/page.tsx
  - app/dashboard/programs/[id]/page.tsx
  - app/dashboard/programs/[id]/edit/page.tsx
  - app/dashboard/customers/[id]/page.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-03
  - DESIGN-05
  - DESIGN-06
  - DESIGN-08
  - DESIGN-09
  - DESIGN-10
  - DESIGN-11

must_haves:
  truths:
    - "Il form crea programma non ha emoji nella selezione tipo (🎫, ⭐, 💰, 👑, 🔄)"
    - "Il dettaglio programma non ha emoji — tipi e sezioni usano icone Lucide"
    - "L'edit programma non ha emoji e tutti gli input hanno focus:border-[#111111]"
    - "Il dettaglio cliente non ha emoji"
    - "Tutti i form inputs in new/edit programma usano il pattern standardizzato DESIGN-08"
    - "Empty states nei dettagli usano EmptyState component"
  artifacts:
    - path: "app/dashboard/programs/new/page.tsx"
      provides: "Form crea programma senza emoji, inputs standardizzati"
    - path: "app/dashboard/programs/[id]/page.tsx"
      provides: "Dettaglio programma senza emoji, tabelle standardizzate"
    - path: "app/dashboard/programs/[id]/edit/page.tsx"
      provides: "Form modifica programma senza emoji, inputs DESIGN-08"
    - path: "app/dashboard/customers/[id]/page.tsx"
      provides: "Dettaglio cliente senza emoji, badges standardizzati"
  key_links:
    - from: "app/dashboard/programs/[id]/page.tsx"
      to: "components/ui/EmptyState.tsx"
      via: "empty state carte/transazioni"
      pattern: "import EmptyState"
    - from: "app/dashboard/programs/new/page.tsx"
      to: "lucide-react"
      via: "icone selezione tipo programma"
      pattern: "from 'lucide-react'"
---

<objective>
Applicare il design system alle 4 pagine più complesse della dashboard: i tre form programma (new, [id], edit) e il dettaglio cliente. Questi file sono i più grandi (1195-1410 linee) e contengono la maggior concentrazione di emoji e stili indigo.

Purpose: Completare la copertura DESIGN-11 — "design system applicato a TUTTE le pagine dashboard". Senza queste 4 pagine, DESIGN-11 non può essere dichiarato soddisfatto.
Output: 4 pagine aggiornate con zero emoji, form inputs standardizzati, empty states corretti.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-design-system-v2/07-RESEARCH.md
@.planning/phases/07-design-system-v2/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Componenti disponibili dopo 07-01 -->

```typescript
import MetricCard from '@/components/ui/MetricCard'
import StatusBadge from '@/components/ui/StatusBadge'
import EmptyState from '@/components/ui/EmptyState'

// Lucide per tipi programma:
import { Stamp, Star, Coins, Crown, RefreshCw } from 'lucide-react'
// Stamp → stamps, Star → points, Coins → cashback, Crown → tiers, RefreshCw → subscription

// Lucide generali:
import { Plus, Trash2, Edit, ArrowLeft, Check, X, Tag,
         Users, CreditCard, TrendingUp, Calendar, Mail, Phone } from 'lucide-react'

// Input standardizzato DESIGN-08:
// className="w-full px-3 py-3 border border-[#E0E0E0] rounded-[8px] text-sm focus:border-[#111111] focus:outline-none transition-colors"

// Select standardizzato:
// className="w-full px-3 py-3 border border-[#E0E0E0] rounded-[8px] text-sm focus:border-[#111111] focus:outline-none transition-colors bg-white"

// Label standardizzata:
// className="block text-sm font-medium text-gray-700 mb-1.5"

// Bottone primario:
// className="bg-[#111111] text-white px-4 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#333333] transition-colors"

// Bottone secondario:
// className="border border-[#E0E0E0] text-gray-700 px-4 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#F5F5F5] transition-colors"

// Bottone pericoloso (elimina):
// className="border border-[#FEE2E2] text-[#DC2626] px-4 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#FEE2E2]/50 transition-colors flex items-center gap-2"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aggiorna programs/new e programs/[id]/edit</name>
  <files>app/dashboard/programs/new/page.tsx, app/dashboard/programs/[id]/edit/page.tsx</files>
  <action>
Leggi entrambi i file prima di modificarli. Sono molto simili — entrambi form per creare/modificare programmi.

**TRASFORMAZIONI COMUNI per new e edit:**

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.

2. STRUTTURA: `<div className="px-6 py-6 max-w-2xl">` (il max-w limita la larghezza del form).

3. PAGE HEADER senza emoji:
   - new: `<h1 className="text-2xl font-semibold text-gray-900">Nuovo Programma</h1>`
   - edit: `<h1 className="text-2xl font-semibold text-gray-900">Modifica Programma</h1>`
   - Entrambi con il link back: `<Link href="/dashboard/programs" className="flex items-center gap-1.5 text-sm text-gray-500 hover:text-gray-900 mb-4"><ArrowLeft size={14} /> Programmi</Link>`

4. SELEZIONE TIPO PROGRAMMA (in new/page.tsx — la sezione con i tile tipo):
   Ogni tile tipo programma sostituisce l'emoji con icona Lucide:
   ```tsx
   // Invece di "🎫 Bollini", usare:
   <div className={`border-2 rounded-[12px] p-4 cursor-pointer transition-all ${
     selectedType === 'stamps' ? 'border-[#111111] bg-gray-50' : 'border-[#E0E0E0] hover:border-gray-400'
   }`} onClick={() => setSelectedType('stamps')}>
     <Stamp size={24} className="mb-2 text-gray-700" />
     <p className="font-semibold text-sm">Bollini</p>
     <p className="text-xs text-gray-500">Una visita = un bollino</p>
   </div>
   ```
   Mappa tipo → icona: stamps→Stamp, points→Star, cashback→Coins, tiers→Crown, subscription→RefreshCw.

5. TUTTI I FORM INPUTS:
   `className="w-full px-3 py-3 border border-[#E0E0E0] rounded-[8px] text-sm focus:border-[#111111] focus:outline-none transition-colors"`
   Rimuovi TUTTI `focus:ring-2 focus:ring-indigo-500`, `border-gray-300`, `rounded-lg` (→ `rounded-[8px]`).

6. TUTTI I SELECT:
   `className="w-full px-3 py-3 border border-[#E0E0E0] rounded-[8px] text-sm focus:border-[#111111] focus:outline-none transition-colors bg-white"`

7. TEXTAREA:
   `className="w-full px-3 py-3 border border-[#E0E0E0] rounded-[8px] text-sm focus:border-[#111111] focus:outline-none transition-colors resize-none"`

8. LABELS:
   `className="block text-sm font-medium text-gray-700 mb-1.5"`

9. SECTION DIVIDERS/CARD: Usa `bg-white border border-[#E8E8E8] rounded-[12px] p-6 mb-4` per raggruppare sezioni form correlate.

10. BOTTONI SUBMIT:
    `className="bg-[#111111] text-white px-6 py-3 rounded-[8px] text-sm font-semibold hover:bg-[#333333] transition-colors"`

11. COLOR PICKER: Se presente input colore, mantieni la logica ma usa `rounded-[8px]` per il preview.

12. UPLOAD LOGO: Mantieni logica esistente (validazione SVG, etc.) — solo UI wrapper aggiornato.

13. LINK/BOTTONI OPZIONALI (Catalogo Premi, Regolamento, Sito Web): Input standardizzati con label.

14. Mantieni TUTTA la logica: validazione, fetch, state management, upload immagini, redirect.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && for f in "app/dashboard/programs/new/page.tsx" "app/dashboard/programs/[id]/edit/page.tsx"; do count=$(grep -c "🎫\|⭐\|💰\|👑\|🔄\|focus:ring\|bg-indigo\|border-gray-300\|rounded-lg" "$f" 2>/dev/null || echo 0); echo "$f: $count da rimuovere"; done</automated>
  </verify>
  <done>programs/new e programs/[id]/edit: zero emoji, tutti gli input con focus:border-[#111111], selezione tipo programma con icone Lucide, bottoni #111111, sezioni form in card bianche.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiorna programs/[id] e customers/[id]</name>
  <files>app/dashboard/programs/[id]/page.tsx, app/dashboard/customers/[id]/page.tsx</files>
  <action>
Leggi entrambi i file prima di modificarli.

**programs/[id]/page.tsx (1410 linee — più complessa):**

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.

2. STRUTTURA: `<div className="px-6 py-6">`.

3. PAGE HEADER:
   ```tsx
   <div className="flex items-center justify-between mb-6">
     <div className="flex items-center gap-3">
       <Link href="/dashboard/programs" className="text-gray-500 hover:text-gray-900">
         <ArrowLeft size={20} />
       </Link>
       <div>
         <h1 className="text-2xl font-semibold text-gray-900">{program.name}</h1>
         <p className="text-sm text-gray-500 mt-0.5">[tipo programma — testo, non emoji]</p>
       </div>
     </div>
     <div className="flex gap-2">
       <Link href={`/dashboard/programs/${program.id}/edit`} className="border border-[#E0E0E0] ...">
         <Edit size={16} /> Modifica
       </Link>
       <button className="border border-[#FEE2E2] text-[#DC2626] ...">
         <Trash2 size={16} /> Elimina
       </button>
     </div>
   </div>
   ```

4. METRICHE PROGRAMMA: Sostituisci card metriche con `<MetricCard>`. Per ogni metrica rimuovi l'emoji e usa icona Lucide come `icon` prop.

5. TABELLA CARTE (lista carte cliente):
   ```tsx
   <div className="bg-white rounded-[12px] border border-[#E8E8E8] overflow-hidden">
     <table className="w-full">
       <thead><tr className="bg-[#F9F9F9] border-b border-[#F0F0F0]">...</tr></thead>
       <tbody>
         {cards.map(card => (
           <tr key={card.id} className="border-b border-[#F0F0F0] last:border-0 hover:bg-gray-50/50">
             ...
           </tr>
         ))}
       </tbody>
     </table>
   </div>
   ```

6. TABELLA TRANSAZIONI: Stesso pattern tabella standardizzata.

7. EMPTY STATES:
   - Nessuna carta: `<EmptyState icon={CreditCard} title="Nessuna carta" description="I clienti appariranno qui dopo l'iscrizione" />`
   - Nessuna transazione: `<EmptyState icon={TrendingUp} title="Nessuna transazione" description="Le transazioni appariranno qui" />`

8. SEZIONE PREMI (rewards): Usa badge/pill senza emoji. Premio con `stamps_required` mostrato come testo.

9. MODALE ELIMINAZIONE: Usa input standardizzato per conferma nome. Bottone conferma: `bg-[#DC2626] text-white hover:bg-red-700`.

10. StatusBadge per stato carta: `<StatusBadge variant={card.status === 'active' ? 'active' : 'inactive'} />`

11. TIPI PROGRAMMA visualizzati come testo con icona Lucide (non emoji): `<Stamp size={14} /> Bollini` ecc.

---

**customers/[id]/page.tsx (625 linee):**

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.

2. STRUTTURA: `<div className="px-6 py-6">`.

3. PAGE HEADER con back link:
   ```tsx
   <Link href="/dashboard/customers" className="flex items-center gap-1.5 text-sm text-gray-500 hover:text-gray-900 mb-4">
     <ArrowLeft size={14} /> Clienti
   </Link>
   <h1 className="text-2xl font-semibold text-gray-900">{customer.full_name}</h1>
   ```

4. INFO CLIENTE: Card `bg-white border border-[#E8E8E8] rounded-[12px] p-6` con:
   - Email: `<Mail size={14} className="text-gray-400" />` invece di 📧
   - Telefono: `<Phone size={14} className="text-gray-400" />` invece di 📱
   - Data iscrizione: `<Calendar size={14} className="text-gray-400" />`

5. TAG CLIENTE: Pill tag senza emoji 🏷️ — usa `<Tag size={12} />`. Input aggiunta tag con classe standardizzata.

6. CARTE DEL CLIENTE: Lista carte con MetricCard o tabella standardizzata per ogni carta.

7. STORICO TRANSAZIONI: Tabella standardizzata senza zebra stripes.

8. EMPTY STATES dove necessario con EmptyState component.

9. BOTTONI: `bg-[#111111]` primari, `border border-[#E0E0E0]` secondari.

10. Mantieni TUTTA la logica esistente — solo UI.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && for f in "app/dashboard/programs/[id]/page.tsx" "app/dashboard/customers/[id]/page.tsx"; do count=$(grep -c "🎫\|⭐\|💰\|👑\|🔄\|📧\|📱\|🏷️\|bg-indigo\|divide-y\|focus:ring" "$f" 2>/dev/null || echo 0); echo "$f: $count da rimuovere"; done</automated>
  </verify>
  <done>programs/[id] e customers/[id]: zero emoji, tabelle standardizzate, MetricCard per metriche, EmptyState per liste vuote, StatusBadge per stati, modale eliminazione con input standardizzato.</done>
</task>

</tasks>

<verification>
Dopo l'esecuzione:
1. `grep -rn "bg-indigo\|focus:ring\|divide-y" app/dashboard/programs/new/page.tsx "app/dashboard/programs/[id]/page.tsx" "app/dashboard/programs/[id]/edit/page.tsx" "app/dashboard/customers/[id]/page.tsx"` — deve tornare vuoto
2. `grep -rn "🎫\|⭐\|💰\|👑\|🔄\|📧\|📱\|🏷️\|🗑️" app/dashboard/programs/ app/dashboard/customers/` — deve tornare vuoto
3. `npm run build` non mostra errori TypeScript per questi file
</verification>

<success_criteria>
- Nessuna emoji nelle 4 pagine
- Tutti i form inputs usano focus:border-[#111111] focus:outline-none
- Selezione tipo programma in new/page.tsx usa icone Lucide (Stamp, Star, Coins, Crown, RefreshCw)
- Tabelle con header bg-[#F9F9F9] e righe border-[#F0F0F0]
- EmptyState usato per ogni lista vuota
- MetricCard usato nelle sezioni metriche
- Tutti i `bg-indigo-*` rimossi
- DESIGN-11 completato: tutte le 11 pagine dashboard aggiornate (7 in 07-02/07-03, 4 in questo piano)
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-v2/07-04-SUMMARY.md`
</output>
