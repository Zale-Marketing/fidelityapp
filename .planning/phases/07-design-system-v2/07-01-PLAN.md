---
phase: 07-design-system-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/globals.css
  - app/dashboard/layout.tsx
  - components/dashboard/Sidebar.tsx
  - components/ui/MetricCard.tsx
  - components/ui/StatusBadge.tsx
  - components/ui/EmptyState.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-07
  - DESIGN-09
  - DESIGN-10

must_haves:
  truths:
    - "La sidebar nera 240px è visibile e fissa su tutte le pagine /dashboard/* su schermi lg+"
    - "L'area contenuto ha sfondo #F5F5F5 applicato dal layout condiviso"
    - "Il font Inter è attivo su tutto il body (non Geist)"
    - "I componenti MetricCard, StatusBadge, EmptyState esistono e sono importabili"
    - "Nessuna pagina pubblica (/, /stamp, /join, /c/[token]) mostra la sidebar"
  artifacts:
    - path: "app/dashboard/layout.tsx"
      provides: "Dashboard layout wrapper con sidebar + content area"
    - path: "components/dashboard/Sidebar.tsx"
      provides: "Sidebar 240px #111111 con nav links Lucide + active state"
    - path: "components/ui/MetricCard.tsx"
      provides: "Card metrica riutilizzabile DESIGN-04"
    - path: "components/ui/StatusBadge.tsx"
      provides: "Badge status active/inactive/expired DESIGN-09"
    - path: "components/ui/EmptyState.tsx"
      provides: "Empty state con icona Lucide 48px DESIGN-10"
  key_links:
    - from: "app/dashboard/layout.tsx"
      to: "components/dashboard/Sidebar.tsx"
      via: "import Sidebar"
      pattern: "import Sidebar from"
    - from: "components/dashboard/Sidebar.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "from 'lucide-react'"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS variable --font-inter"
      pattern: "--font-inter"
---

<objective>
Creare le fondamenta del design system: installare lucide-react, creare il dashboard layout condiviso con sidebar nera, i 3 componenti UI riutilizzabili, e aggiornare il font da Geist a Inter.

Purpose: Tutti i piani successivi (07-02, 07-03, 07-04) dipendono da questi artefatti. Senza layout condiviso e componenti, le 11 pagine non possono essere aggiornate consistentemente.
Output: `app/dashboard/layout.tsx`, `components/dashboard/Sidebar.tsx`, `components/ui/{MetricCard,StatusBadge,EmptyState}.tsx`, font Inter attivo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-design-system-v2/07-RESEARCH.md
</context>

<interfaces>
<!-- Contratti che questo piano stabilisce e che i piani 07-02, 07-03, 07-04 useranno -->

```typescript
// components/ui/MetricCard.tsx
interface MetricCardProps {
  label: string
  value: string | number
  icon?: React.ReactNode
  valueClassName?: string
}
export default function MetricCard(props: MetricCardProps): JSX.Element

// components/ui/StatusBadge.tsx
type BadgeVariant = 'active' | 'inactive' | 'expired' | 'pending'
export default function StatusBadge({ variant, label }: { variant: BadgeVariant; label?: string }): JSX.Element

// components/ui/EmptyState.tsx
import { LucideIcon } from 'lucide-react'
interface EmptyStateProps {
  icon: LucideIcon
  title: string
  description: string
  actionLabel?: string
  actionHref?: string
}
export default function EmptyState(props: EmptyStateProps): JSX.Element
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Installa lucide-react + aggiorna font Inter</name>
  <files>package.json, app/layout.tsx, app/globals.css</files>
  <action>
1. Esegui `npm install lucide-react` nella root del progetto.

2. Aggiorna `app/layout.tsx`:
   - Sostituisci l'import di `Geist` e `Geist_Mono` con `Inter` da `next/font/google`
   - Configura Inter: `const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })`
   - Nel `<body>`, usa `className={`${inter.variable} font-sans antialiased`}` (rimuovi le variabili geist)
   - Aggiorna metadata: title "FidelityApp" e description "Gestisci il tuo programma fedeltà"

3. Aggiorna `app/globals.css`:
   - Trova la regola `body { ... }` (o aggiungila se non c'è)
   - Imposta `font-family: var(--font-inter), system-ui, sans-serif;`
   - Rimuovi qualsiasi riferimento a `--font-geist-sans` o `var(--font-geist-sans)` nel body
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && node -e "require('./node_modules/lucide-react')" && echo "lucide-react OK"</automated>
  </verify>
  <done>lucide-react installato, app/layout.tsx usa Inter, globals.css ha font-family Inter</done>
</task>

<task type="auto">
  <name>Task 2: Crea dashboard layout + Sidebar component</name>
  <files>app/dashboard/layout.tsx, components/dashboard/Sidebar.tsx</files>
  <action>
Crea `components/dashboard/Sidebar.tsx` (aggiungere directory `components/dashboard/` se non esiste):

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { Home, Target, Users, Bell, BarChart2, CreditCard, Settings, ScanLine } from 'lucide-react'

const NAV_ITEMS = [
  { href: '/dashboard', icon: Home, label: 'Dashboard' },
  { href: '/dashboard/programs', icon: Target, label: 'Programmi' },
  { href: '/dashboard/customers', icon: Users, label: 'Clienti' },
  { href: '/dashboard/notifications', icon: Bell, label: 'Notifiche' },
  { href: '/dashboard/analytics', icon: BarChart2, label: 'Analytics' },
  { href: '/dashboard/billing', icon: CreditCard, label: 'Abbonamento' },
  { href: '/dashboard/settings', icon: Settings, label: 'Impostazioni' },
]

export default function Sidebar() {
  const pathname = usePathname()

  return (
    <aside className="fixed left-0 top-0 h-full w-[240px] bg-[#111111] flex flex-col z-40">
      <div className="px-6 py-5 border-b border-white/10">
        <p className="text-white font-semibold text-lg">FidelityApp</p>
      </div>
      <nav className="flex-1 px-3 py-4 space-y-1 overflow-y-auto">
        {NAV_ITEMS.map(({ href, icon: Icon, label }) => {
          const isActive = href === '/dashboard'
            ? pathname === '/dashboard'
            : pathname.startsWith(href)
          return (
            <Link
              key={href}
              href={href}
              className={`flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-colors ${
                isActive
                  ? 'bg-[#2A2A2A] text-white'
                  : 'text-white/70 hover:bg-[#1E1E1E] hover:text-white'
              }`}
            >
              <Icon size={18} />
              {label}
            </Link>
          )
        })}
      </nav>
      <div className="px-3 pb-4 border-t border-white/10 pt-3">
        <Link
          href="/stamp"
          className="flex items-center gap-3 px-3 py-2.5 bg-white text-[#111111] rounded-lg text-sm font-semibold hover:bg-gray-100 transition-colors"
        >
          <ScanLine size={18} />
          Apri Scanner
        </Link>
      </div>
    </aside>
  )
}
```

Crea `app/dashboard/layout.tsx` — questo file NON deve contenere `<html>` o `<body>` (nested layout Next.js):

```tsx
import Sidebar from '@/components/dashboard/Sidebar'

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="flex min-h-screen bg-[#F5F5F5]">
      <Sidebar />
      <main className="flex-1 ml-[240px] min-h-screen">
        {children}
      </main>
    </div>
  )
}
```

IMPORTANTE: Il `ml-[240px]` compensa la sidebar fixed. Il layout NON reiplica `<html>/<body>`.
  </action>
  <verify>
    <automated>ls /c/Users/Zanni/fidelityapp/app/dashboard/layout.tsx /c/Users/Zanni/fidelityapp/components/dashboard/Sidebar.tsx && echo "Files exist OK"</automated>
  </verify>
  <done>app/dashboard/layout.tsx e components/dashboard/Sidebar.tsx esistono con il codice corretto. Sidebar ha `fixed left-0 w-[240px] bg-[#111111]`, layout ha `ml-[240px] bg-[#F5F5F5]`.</done>
</task>

<task type="auto">
  <name>Task 3: Crea componenti UI riutilizzabili (MetricCard, StatusBadge, EmptyState)</name>
  <files>components/ui/MetricCard.tsx, components/ui/StatusBadge.tsx, components/ui/EmptyState.tsx</files>
  <action>
Crea la directory `components/ui/` se non esiste (contiene già `LeadForm.tsx` — non toccare).

Crea `components/ui/MetricCard.tsx`:
```tsx
interface MetricCardProps {
  label: string
  value: string | number
  icon?: React.ReactNode
  valueClassName?: string
  trend?: string
}

export default function MetricCard({ label, value, icon, valueClassName, trend }: MetricCardProps) {
  return (
    <div className="bg-white border border-[#E8E8E8] rounded-[12px] p-6 shadow-[0_1px_3px_rgba(0,0,0,0.08)]">
      {icon && <div className="mb-3 text-[#D1D5DB]">{icon}</div>}
      <p className="text-sm text-gray-500">{label}</p>
      <p className={`text-3xl font-bold text-gray-900 mt-1 ${valueClassName || ''}`}>{value}</p>
      {trend && <p className="text-xs text-gray-400 mt-1">{trend}</p>}
    </div>
  )
}
```

Crea `components/ui/StatusBadge.tsx`:
```tsx
type BadgeVariant = 'active' | 'inactive' | 'expired' | 'pending'

const BADGE_STYLES: Record<BadgeVariant, string> = {
  active: 'bg-[#DCFCE7] text-[#16A34A]',
  inactive: 'bg-gray-100 text-gray-500',
  expired: 'bg-[#FEE2E2] text-[#DC2626]',
  pending: 'bg-yellow-100 text-yellow-700',
}

const BADGE_LABELS: Record<BadgeVariant, string> = {
  active: 'Attivo',
  inactive: 'Inattivo',
  expired: 'Scaduto',
  pending: 'In attesa',
}

export default function StatusBadge({ variant, label }: { variant: BadgeVariant; label?: string }) {
  return (
    <span className={`inline-flex px-2.5 py-1 rounded-full text-xs font-medium ${BADGE_STYLES[variant]}`}>
      {label || BADGE_LABELS[variant]}
    </span>
  )
}
```

Crea `components/ui/EmptyState.tsx`:
```tsx
import { LucideIcon } from 'lucide-react'
import Link from 'next/link'

interface EmptyStateProps {
  icon: LucideIcon
  title: string
  description: string
  actionLabel?: string
  actionHref?: string
  onAction?: () => void
}

export default function EmptyState({ icon: Icon, title, description, actionLabel, actionHref, onAction }: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-16 px-8 text-center">
      <Icon size={48} className="text-[#D1D5DB] mb-4" strokeWidth={1.5} />
      <h3 className="text-lg font-semibold text-gray-900 mb-2">{title}</h3>
      <p className="text-sm text-gray-500 max-w-sm">{description}</p>
      {actionLabel && actionHref && (
        <Link
          href={actionHref}
          className="mt-6 bg-[#111111] text-white px-5 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#333333] transition-colors"
        >
          {actionLabel}
        </Link>
      )}
      {actionLabel && onAction && !actionHref && (
        <button
          onClick={onAction}
          className="mt-6 bg-[#111111] text-white px-5 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#333333] transition-colors"
        >
          {actionLabel}
        </button>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>ls /c/Users/Zanni/fidelityapp/components/ui/MetricCard.tsx /c/Users/Zanni/fidelityapp/components/ui/StatusBadge.tsx /c/Users/Zanni/fidelityapp/components/ui/EmptyState.tsx && echo "All UI components OK"</automated>
  </verify>
  <done>I tre componenti UI esistono con le interfacce corrette. MetricCard ha border/shadow DESIGN-04. StatusBadge ha i colori esatti DESIGN-09. EmptyState usa LucideIcon 48px DESIGN-10.</done>
</task>

</tasks>

<verification>
Dopo l'esecuzione verificare:
1. `npm run build` (o `npm run dev`) non mostra errori TypeScript nei file creati
2. La sidebar appare su `/dashboard` ma NON su `/` o `/stamp` (layout scoping Next.js corretto)
3. `grep -r "lucide-react" components/dashboard/Sidebar.tsx` trova gli import
4. `grep "font-inter" app/layout.tsx` trova la variabile CSS
</verification>

<success_criteria>
- lucide-react installato in node_modules
- app/dashboard/layout.tsx crea il wrapper flex con sidebar + main ml-[240px]
- Sidebar.tsx: fixed 240px bg-[#111111], 7 nav links con icone Lucide, active state usePathname
- MetricCard, StatusBadge, EmptyState esistono in components/ui/ con contratti TypeScript corretti
- Font Inter attivo — app/layout.tsx usa `Inter` da next/font/google
- ZERO pagine pubbliche (/, /stamp, /c, /join) impattate dalla sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-v2/07-01-SUMMARY.md`
</output>
