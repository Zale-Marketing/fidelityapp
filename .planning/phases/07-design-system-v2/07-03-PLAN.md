---
phase: 07-design-system-v2
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/dashboard/analytics/page.tsx
  - app/dashboard/billing/page.tsx
  - app/dashboard/customers/page.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-05
  - DESIGN-06
  - DESIGN-08
  - DESIGN-11

must_haves:
  truths:
    - "Analytics page non contiene emoji — tipi programma mostrati con icone Lucide"
    - "Billing page non contiene emoji — check/X mostrati con icone Lucide"
    - "Customers list non contiene emoji — tag e icone azioni usano Lucide"
    - "Tabella clienti ha header #F9F9F9 e righe senza zebra stripes"
    - "Badge status clienti usa StatusBadge component"
  artifacts:
    - path: "app/dashboard/analytics/page.tsx"
      provides: "Analytics senza emoji, bottoni #111111"
    - path: "app/dashboard/billing/page.tsx"
      provides: "Billing senza emoji (✓/✗), Check/X Lucide"
    - path: "app/dashboard/customers/page.tsx"
      provides: "Lista clienti senza emoji, tabella standardizzata"
  key_links:
    - from: "app/dashboard/customers/page.tsx"
      to: "components/ui/StatusBadge.tsx"
      via: "import StatusBadge"
      pattern: "import StatusBadge"
    - from: "app/dashboard/customers/page.tsx"
      to: "components/ui/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "import EmptyState"
---

<objective>
Applicare il design system alle 3 pagine di media complessità: analytics (411 linee), billing (388 linee), customers list (779 linee). Rimuovere tutte le emoji, standardizzare tabelle e bottoni.

Purpose: Queste 3 pagine eseguono in parallelo con 07-02 e 07-04 — nessun file condiviso.
Output: 3 pagine aggiornate con zero emoji, token #111111, tabelle standardizzate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-design-system-v2/07-RESEARCH.md
@.planning/phases/07-design-system-v2/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Componenti disponibili dopo 07-01 -->

```typescript
// Imports disponibili:
import MetricCard from '@/components/ui/MetricCard'
import StatusBadge from '@/components/ui/StatusBadge'
import EmptyState from '@/components/ui/EmptyState'

// Lucide icons:
import { BarChart2, TrendingUp, Users, CreditCard, Check, X,
         Tag, Mail, Phone, Search, Plus, Trash2, Star, Coins,
         Crown, RefreshCw, Stamp } from 'lucide-react'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aggiorna analytics page e billing page</name>
  <files>app/dashboard/analytics/page.tsx, app/dashboard/billing/page.tsx</files>
  <action>
Leggi entrambi i file prima di modificarli. Poi applica:

**analytics/page.tsx (411 linee):**

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.
2. STRUTTURA: `<div className="px-6 py-6">`.
3. PAGE HEADER senza emoji:
   ```tsx
   <h1 className="text-2xl font-semibold text-gray-900">Analytics</h1>
   <p className="text-sm text-gray-500 mt-1">Statistiche del programma fedeltà</p>
   ```
4. METRICHE: Sostituisci card metriche con `<MetricCard>`. Usa icone Lucide per `icon` prop:
   - Clienti totali → `<Users size={20} />`
   - Transazioni → `<TrendingUp size={20} />`
   - Tipi programma → rimuovi emoji (📊, 🎫, ⭐, 💰, 👑, 🔄, 📈), usa icone Lucide o solo testo
5. TABELLE (se presenti): Applica pattern standardizzato `bg-[#F9F9F9]` header, `border-[#F0F0F0]` righe.
6. BOTTONI: `bg-[#111111] hover:bg-[#333333]` — rimuovi `bg-indigo-*`.
7. Mantieni grafici/chart esistenti invariati (non toccare logica recharts se presente).
8. CARD SEZIONI: Usa `bg-white border border-[#E8E8E8] rounded-[12px] p-6 shadow-[0_1px_3px_rgba(0,0,0,0.08)]` per sezioni card.

**billing/page.tsx (388 linee):**

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.
2. STRUTTURA: `<div className="px-6 py-6">`.
3. PAGE HEADER senza emoji:
   ```tsx
   <h1 className="text-2xl font-semibold text-gray-900">Abbonamento</h1>
   ```
4. ICONE CHECK/X: I caratteri ✓ e ✗ (e le emoji 🚀, ✅) sono i problemi principali in questa pagina.
   - ✓ / ✅ → `<Check size={16} className="text-[#16A34A]" />`
   - ✗ → `<X size={16} className="text-[#DC2626]" />`
   - 🚀 → `<Rocket size={16} />` oppure rimuovere se solo decorativa
5. PLAN CARDS: Usa `bg-white border border-[#E8E8E8] rounded-[12px] p-6` per le card piano.
6. BOTTONI: `bg-[#111111] hover:bg-[#333333] rounded-[8px]` — rimuovi `bg-indigo-*`.
7. BADGE STATUS piano: Usa `<StatusBadge variant="active" />` per piano attivo.
8. Mantieni tutta la logica Stripe/piano invariata.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && for f in app/dashboard/analytics/page.tsx app/dashboard/billing/page.tsx; do count=$(grep -c "📊\|🎫\|⭐\|💰\|👑\|🔄\|📈\|🚀\|✓\|✗\|✅\|❌\|bg-indigo" "$f" 2>/dev/null || echo 0); echo "$f: $count occorrenze da rimuovere"; done</automated>
  </verify>
  <done>analytics/page.tsx e billing/page.tsx: zero emoji e zero bg-indigo-*, MetricCard usato per metriche, Check/X Lucide per check list billing.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiorna customers list (app/dashboard/customers/page.tsx)</name>
  <files>app/dashboard/customers/page.tsx</files>
  <action>
Leggi il file prima di modificarlo (779 linee — pagina complessa con filtri tag, tabella clienti, CSV export). Poi applica:

1. RIMUOVI wrapper `min-h-screen bg-gray-*` standalone.

2. STRUTTURA: `<div className="px-6 py-6">`.

3. PAGE HEADER:
   ```tsx
   <div className="flex items-center justify-between mb-6">
     <div>
       <h1 className="text-2xl font-semibold text-gray-900">Clienti</h1>
       <p className="text-sm text-gray-500 mt-1">Gestisci i tuoi clienti</p>
     </div>
     <button className="bg-[#111111] text-white px-4 py-2.5 rounded-[8px] text-sm font-medium hover:bg-[#333333] transition-colors flex items-center gap-2">
       <Download size={16} />
       Esporta CSV
     </button>
   </div>
   ```

4. RICERCA/FILTRI: L'input di ricerca usa:
   `className="border border-[#E0E0E0] rounded-[8px] px-3 py-2.5 text-sm focus:border-[#111111] focus:outline-none transition-colors w-full"`
   Icona `<Search size={16} />` invece di 🔍.

5. FILTRO TAG: I tag pill/badge usano `bg-gray-100 text-gray-600 hover:bg-gray-200 rounded-full px-3 py-1 text-xs font-medium`. Icona `<Tag size={12} />` invece di 🏷️.

6. TABELLA CLIENTI (standardizzata):
   ```tsx
   <div className="bg-white rounded-[12px] border border-[#E8E8E8] overflow-hidden">
     <table className="w-full">
       <thead>
         <tr className="bg-[#F9F9F9] border-b border-[#F0F0F0]">
           <th className="text-left px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wide">Cliente</th>
           {/* altre colonne */}
         </tr>
       </thead>
       <tbody>
         {customers.map(c => (
           <tr key={c.id} className="border-b border-[#F0F0F0] last:border-0 hover:bg-gray-50/50">
             <td className="px-6 py-4">...</td>
           </tr>
         ))}
       </tbody>
     </table>
   </div>
   ```
   RIMUOVI: `divide-y`, zebra stripes (`bg-gray-50` alternato).

7. ICONE NELLA TABELLA:
   - 📧 → `<Mail size={14} className="text-gray-400" />`
   - 📱 → `<Phone size={14} className="text-gray-400" />`
   - 🏷️ → `<Tag size={14} className="text-gray-400" />`
   - ✅ → `<Check size={14} className="text-[#16A34A]" />`
   - ➕ → `<Plus size={14} />` nel pulsante "Aggiungi tag"

8. EMPTY STATE: Sostituisci stato vuoto clienti con:
   ```tsx
   <EmptyState
     icon={Users}
     title="Nessun cliente"
     description="I clienti appariranno qui quando si iscrivono al tuo programma"
   />
   ```

9. BOTTONI: Tutti `bg-[#111111]` primari, `border border-[#E0E0E0]` secondari.

10. Mantieni TUTTA la logica: filtri tag, CSV export, paginazione, fetch clienti — solo UI cambia.
  </action>
  <verify>
    <automated>cd /c/Users/Zanni/fidelityapp && grep -c "👥\|📧\|📱\|🏷️\|✅\|➕\|bg-indigo\|divide-y" app/dashboard/customers/page.tsx 2>/dev/null || echo "0"</automated>
  </verify>
  <done>customers/page.tsx: zero emoji, tabella con header #F9F9F9 senza zebra, icone Lucide per email/phone/tag, EmptyState per lista vuota, bottoni #111111.</done>
</task>

</tasks>

<verification>
Dopo l'esecuzione:
1. `grep -rn "bg-indigo\|text-indigo" app/dashboard/analytics/page.tsx app/dashboard/billing/page.tsx app/dashboard/customers/page.tsx` — deve tornare vuoto
2. `grep -rn "✓\|✗\|✅\|❌\|🚀\|📊\|👥\|📧\|📱" app/dashboard/analytics/page.tsx app/dashboard/billing/page.tsx app/dashboard/customers/page.tsx` — deve tornare vuoto
3. `npm run build` non mostra errori TypeScript per questi file
</verification>

<success_criteria>
- Nessuna emoji in analytics/page.tsx, billing/page.tsx, customers/page.tsx
- Tabella customers: header bg-[#F9F9F9], righe border-[#F0F0F0], nessun divide-y
- billing/page.tsx usa Check e X da lucide-react (non ✓ ✗ Unicode)
- EmptyState usato in customers per lista vuota
- StatusBadge usato per status attivo/inattivo clienti/abbonamento
- Nessun wrapper min-h-screen bg-gray-* standalone
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-v2/07-03-SUMMARY.md`
</output>
