---
phase: 16-dashboard-alert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/ocio/reviews/[id]/route.ts
  - trigger/ocio-ai-analyzer.ts
  - app/dashboard/ocio/settings/page.tsx
autonomous: true
requirements:
  - ALERT-01
  - ALERT-02
  - ALERT-03

must_haves:
  truths:
    - "Merchant può segnare una recensione come 'replied' o 'ignored' — la UI riflette il cambio"
    - "Task ocio-ai-analyzer invia WhatsApp al numero alert quando sentiment=negative o urgency in high/critical, solo se module_alerts=true e alert_whatsapp_number non null"
    - "Merchant configura alert_whatsapp_number nella settings page OCIO e il valore persiste dopo ricarica"
  artifacts:
    - path: "app/api/ocio/reviews/[id]/route.ts"
      provides: "PATCH endpoint per aggiornare reply_status su ocio_reviews"
      exports: ["PATCH"]
    - path: "trigger/ocio-ai-analyzer.ts"
      provides: "Logica alert WhatsApp per recensioni negative/urgenti dopo analisi AI"
      contains: "sendTextMessage"
    - path: "app/dashboard/ocio/settings/page.tsx"
      provides: "Form alert_whatsapp_number nel modulo Alert WhatsApp"
      contains: "alert_whatsapp_number"
  key_links:
    - from: "trigger/ocio-ai-analyzer.ts"
      to: "lib/sendapp.ts"
      via: "sendTextMessage(number, message, instanceId, accessToken)"
      pattern: "sendTextMessage"
    - from: "trigger/ocio-ai-analyzer.ts"
      to: "ocio_reviews"
      via: "UPDATE alert_sent=true, alert_sent_at=now()"
      pattern: "alert_sent"
    - from: "app/api/ocio/reviews/[id]/route.ts"
      to: "ocio_reviews"
      via: "UPDATE reply_status, replied_at"
      pattern: "reply_status"
---

<objective>
Backend per la dashboard OCIO: API PATCH per aggiornare lo stato risposta di una recensione, logica alert WhatsApp nel task AI Trigger.dev, e campo numero alert nella settings page.

Purpose: Queste tre modifiche sono i prerequisiti che il piano 02 consumerà — l'API PATCH serve al modal della dashboard, gli alert servono al flusso automatico post-analisi AI.
Output: route PATCH /api/ocio/reviews/[id], alert WA in ocio-ai-analyzer.ts, campo alert_whatsapp_number in settings OCIO
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-dashboard-alert/16-CONTEXT.md

@app/api/ocio/config/route.ts
@trigger/ocio-ai-analyzer.ts
@lib/sendapp.ts
@lib/types.ts
@app/dashboard/ocio/settings/page.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/types.ts:
```typescript
export type OcioReview = {
  id: string
  merchant_id: string
  rating: number | null
  text: string | null
  author_name: string | null
  review_url: string | null
  ai_sentiment: 'positive' | 'neutral' | 'negative' | null
  ai_urgency: 'low' | 'medium' | 'high' | 'critical' | null
  ai_suggested_reply: string | null
  reply_status: 'pending' | 'replied' | 'ignored'
  replied_at: string | null
  alert_sent: boolean
  alert_sent_at: string | null
}

export type OcioConfig = {
  merchant_id: string
  module_alerts: boolean
  alert_whatsapp_number: string | null
  // merchants table (joined separately):
  // sendapp_instance_id, sendapp_access_token, sendapp_status
}
```

From lib/sendapp.ts:
```typescript
export async function sendTextMessage(
  number: string,       // normalized phone e.g. "393331234567"
  message: string,
  instanceId: string,
  accessToken: string
): Promise<any>

export function formatPhoneIT(phone: string): string | null
```

Pattern API auth (from app/api/ocio/config/route.ts):
```typescript
// Verify auth header
const authHeader = request.headers.get('Authorization')
const token = authHeader?.replace('Bearer ', '')
const { data: { user }, error: authError } = await supabase.auth.getUser(token)
if (authError || !user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

// Verify merchant + plan
const { data: profile } = await supabase.from('profiles').select('merchant_id').eq('id', user.id).single()
const { data: merchant } = await supabase.from('merchants').select('plan').eq('id', profile.merchant_id).single()
if (merchant?.plan?.toLowerCase() !== 'business') return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
```

From trigger/ocio-ai-analyzer.ts (current structure):
```typescript
// After the for loop, before returning summary:
// Add WhatsApp alert logic here (per-review, inside the try block after successful UPDATE)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PATCH /api/ocio/reviews/[id] — aggiorna reply_status</name>
  <files>app/api/ocio/reviews/[id]/route.ts</files>
  <action>
Crea la route PATCH che aggiorna `reply_status` (e `replied_at` se 'replied') su `ocio_reviews`.

Struttura:
- Crea la directory `app/api/ocio/reviews/[id]/` e il file `route.ts`
- Auth: Bearer token via `supabase.auth.getUser(token)` — pattern identico a `app/api/ocio/config/route.ts`
- Verifica piano BUSINESS (merchant.plan.toLowerCase() !== 'business' → 403)
- Leggi `{ reply_status }` dal body — valori validi: 'pending' | 'replied' | 'ignored'; se non valido → 400
- Leggi `id` da `params` (route param `[id]`)
- Verifica ownership: `SELECT merchant_id FROM ocio_reviews WHERE id = $id` — se `merchant_id !== profile.merchant_id` → 403
- UPDATE: `UPDATE ocio_reviews SET reply_status = $reply_status, replied_at = CASE WHEN $reply_status = 'replied' THEN now() ELSE replied_at END WHERE id = $id`
  - In Supabase: `.update({ reply_status, ...(reply_status === 'replied' ? { replied_at: new Date().toISOString() } : {}) }).eq('id', id).eq('merchant_id', profile.merchant_id)`
- Risposta: 200 `{ success: true }`
- Usa `createClient(SUPABASE_URL, SERVICE_ROLE_KEY)` (server-side, service role)
  </action>
  <verify>
    <automated>curl -X PATCH http://localhost:3000/api/ocio/reviews/test-id -H "Content-Type: application/json" -d '{"reply_status":"replied"}' | grep -E "401|Unauthorized"</automated>
  </verify>
  <done>Route risponde 401 senza auth header, 400 con reply_status non valido, 200 con aggiornamento corretto su DB verificato da Supabase</done>
</task>

<task type="auto">
  <name>Task 2: Alert WhatsApp in ocio-ai-analyzer + campo settings</name>
  <files>trigger/ocio-ai-analyzer.ts, app/dashboard/ocio/settings/page.tsx</files>
  <action>
**Parte A — trigger/ocio-ai-analyzer.ts:**

Aggiungi la logica alert WhatsApp dentro il `for` loop, subito dopo il blocco `if (updateError)` / `else { processed++ }`, usando un `try/catch` separato che NON fallisce il task se SendApp fallisce.

Logica da aggiungere nel blocco `else { processed++ }`:

```typescript
// Alert WhatsApp — separato, non fa fallire il task
try {
  const shouldAlert =
    result.sentiment === 'negative' ||
    result.urgency === 'high' ||
    result.urgency === 'critical'

  if (shouldAlert && !review_row.alert_sent) {
    // Fetch ocio_config for this merchant (una sola volta fuori dal loop è meglio,
    // ma per semplicità fetch dentro il loop con early-exit)
    const { data: alertConfig } = await supabase
      .from('ocio_config')
      .select('module_alerts, alert_whatsapp_number')
      .eq('merchant_id', merchantId)
      .single()

    if (alertConfig?.module_alerts && alertConfig?.alert_whatsapp_number) {
      // Fetch merchant SendApp credentials
      const { data: merchantData } = await supabase
        .from('merchants')
        .select('sendapp_instance_id, sendapp_access_token, sendapp_status')
        .eq('id', merchantId)
        .single()

      if (
        merchantData?.sendapp_status === 'connected' &&
        merchantData?.sendapp_instance_id &&
        merchantData?.sendapp_access_token
      ) {
        const reviewText = review.text ?? ''
        const excerpt = reviewText.length > 100 ? reviewText.slice(0, 100) + '...' : reviewText
        const authorName = review_row.author_name ?? 'Anonimo'
        const rating = review_row.rating ?? '?'
        const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'https://fidelityapp-six.vercel.app'

        const message = `⭐ Nuova recensione ${rating}/5 da ${authorName}\n"${excerpt}"\n\nVisualizza su OCIO: ${appUrl}/dashboard/ocio`

        const { sendTextMessage, formatPhoneIT } = await import('@/lib/sendapp')
        // Note: in Trigger.dev context, use direct import at top of file instead of dynamic import
        // Struttura finale: import { sendTextMessage, formatPhoneIT } from '../lib/sendapp' in cima al file

        const normalizedPhone = formatPhoneIT(alertConfig.alert_whatsapp_number)
        if (normalizedPhone) {
          await sendTextMessage(
            normalizedPhone,
            message,
            merchantData.sendapp_instance_id,
            merchantData.sendapp_access_token
          )

          await supabase
            .from('ocio_reviews')
            .update({ alert_sent: true, alert_sent_at: new Date().toISOString() })
            .eq('id', review.id)
        }
      }
    }
  }
} catch (alertErr) {
  logger.error('WhatsApp alert failed (non-critical)', {
    reviewId: review.id,
    error: alertErr instanceof Error ? alertErr.message : String(alertErr),
  })
}
```

IMPORTANTE:
- Importa `sendTextMessage` e `formatPhoneIT` da `'../lib/sendapp'` (import statico in cima al file, non dinamico)
- Aggiungi `author_name`, `rating`, `review_url` alla SELECT iniziale delle recensioni (attualmente seleziona solo `id, text, rating`) — aggiorna la query a `.select('id, text, rating, author_name, review_url, alert_sent')`
- Aggiorna il tipo `OcioReviewRow` per includere `author_name: string | null`, `review_url: string | null`, `alert_sent: boolean`
- Il fetch di `ocio_config` per gli alert può essere fatto UNA SOLA VOLTA prima del `for` loop e riusato — ottimizzazione opzionale ma preferita per ridurre query DB
- `review_row` nella pseudocode sopra è la variabile `review` del loop (rename mentale per chiarezza — usa semplicemente `review`)

**Parte B — app/dashboard/ocio/settings/page.tsx:**

Aggiungi il campo `alert_whatsapp_number` al form della settings page. Il campo deve apparire solo quando `module_alerts` è attivo.

1. Aggiungi `alertPhone` allo state: `const [alertPhone, setAlertPhone] = useState<string>('')`
2. In `loadData()`, dopo `setMapsUrl(data.google_maps_url ?? '')`, aggiungi: `setAlertPhone(data.alert_whatsapp_number ?? '')`
3. In `saveConfig()`, aggiungi `alert_whatsapp_number: alertPhone.trim() || null` al body della PATCH
4. Nella UI, nel modulo "Alert WhatsApp" (già esistente come card), aggiungi sotto la descrizione del modulo (solo se `config.module_alerts`):

```tsx
{config.module_alerts && (
  <div className="mt-3">
    <label className="text-xs text-gray-500 block mb-1">
      Numero WhatsApp per alert (es. +39333...):
    </label>
    <input
      type="tel"
      value={alertPhone}
      onChange={e => setAlertPhone(e.target.value)}
      placeholder="+393331234567"
      className="w-full border border-[#E0E0E0] rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-black"
    />
    <p className="text-xs text-gray-400 mt-1">
      Riceverai un WhatsApp per ogni recensione negativa o urgente.
    </p>
  </div>
)}
```

Questo numero è separato dal numero SendApp business — è il destinatario dell'alert, non il mittente.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ocio-ai-analyzer|settings" | head -10</automated>
  </verify>
  <done>TypeScript compila senza errori nei file modificati; il task ocio-ai-analyzer seleziona author_name/review_url/alert_sent; la settings page mostra il campo alertPhone quando module_alerts è true</done>
</task>

</tasks>

<verification>
- `app/api/ocio/reviews/[id]/route.ts` esiste e gestisce PATCH
- `trigger/ocio-ai-analyzer.ts` importa `sendTextMessage, formatPhoneIT` da `'../lib/sendapp'` e ha la logica alert nel loop
- `app/dashboard/ocio/settings/page.tsx` ha stato `alertPhone`, lo carica da API, lo invia in PATCH, e mostra l'input quando `module_alerts` è true
- TypeScript compila senza errori: `npx tsc --noEmit`
</verification>

<success_criteria>
- PATCH /api/ocio/reviews/[id] risponde 401 senza auth, 403 senza piano BUSINESS, 200 su update valido
- ocio-ai-analyzer invia WhatsApp alert solo quando: sentiment=negative OR urgency in [high, critical]; module_alerts=true; alert_whatsapp_number not null; sendapp_status=connected; alert_sent=false
- Settings page salva alert_whatsapp_number in PATCH /api/ocio/config e lo mostra ricaricando la pagina
- Fallimento SendApp loggato ma non propaga eccezione (task AI non ritenta per colpa dell'alert)
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-alert/16-01-SUMMARY.md`
</output>
