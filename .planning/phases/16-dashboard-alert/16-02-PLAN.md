---
phase: 16-dashboard-alert
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - app/dashboard/ocio/page.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Merchant vede 4 KPI in riga (rating medio, totale recensioni, nuove 30gg, da rispondere) + ComposedChart 6 mesi"
    - "Merchant filtra recensioni per sentiment (positive/neutral/negative), rating (1-5 stelle), periodo (30/90/tutto)"
    - "Ogni card recensione mostra stelle colorate, autore, data, testo troncato (line-clamp-3), badge sentiment, badge urgenza, max 2 badge temi"
    - "Merchant clicca 'Vedi risposta AI' e si apre il modal con testo completo, analisi AI, banner fake se present, risposta AI con pulsante Copia"
    - "Copia risposta: toast 'Copiato!', pulsante diventa verde 2s, modal rimane aperto"
    - "Merchant clicca 'Ho risposto' o 'Ignora' nel modal — PATCH /api/ocio/reviews/[id] aggiorna reply_status; modal chiude e card aggiorna badge"
  artifacts:
    - path: "app/dashboard/ocio/page.tsx"
      provides: "Dashboard OCIO completa: KPI + chart + lista filtrata + modal"
      min_lines: 300
      contains: "ComposedChart"
  key_links:
    - from: "app/dashboard/ocio/page.tsx"
      to: "/api/ocio/reviews/[id]"
      via: "PATCH fetch per reply_status update"
      pattern: "reply_status"
    - from: "app/dashboard/ocio/page.tsx"
      to: "ocio_reviews"
      via: "SELECT * FROM ocio_reviews WHERE merchant_id = $id ORDER BY published_at DESC"
      pattern: "ocio_reviews"
---

<objective>
Dashboard OCIO completa a `app/dashboard/ocio/page.tsx`: sostituisce completamente lo stub con 4 KPI, ComposedChart trend 6 mesi, lista recensioni filtrata con cards compatte, e modal recensione con analisi AI + copia risposta + update reply_status.

Purpose: Questo è il deliverable principale della fase — il merchant finalmente vede e gestisce tutte le proprie recensioni con le analisi AI generate nelle fasi precedenti.
Output: app/dashboard/ocio/page.tsx riscritta completamente (~350+ righe)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dashboard-alert/16-CONTEXT.md
@.planning/phases/16-dashboard-alert/16-01-SUMMARY.md

@app/dashboard/ocio/page.tsx
@lib/types.ts
@components/ui/MetricCard.tsx
@components/ui/EmptyState.tsx
@components/ui/UpgradePrompt.tsx
@app/dashboard/analytics/page.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/types.ts:
```typescript
export type OcioReview = {
  id: string
  merchant_id: string
  review_id: string
  author_name: string | null
  rating: number | null
  text: string | null
  published_at: string | null
  review_url: string | null
  ai_sentiment: 'positive' | 'neutral' | 'negative' | null
  ai_score: number | null
  ai_themes: string[] | null
  ai_urgency: 'low' | 'medium' | 'high' | 'critical' | null
  ai_is_fake: boolean
  ai_fake_reason: string | null
  ai_suggested_reply: string | null
  ai_analyzed_at: string | null
  reply_status: 'pending' | 'replied' | 'ignored'
  replied_at: string | null
  alert_sent: boolean
  created_at: string
}

export type OcioConfig = {
  google_maps_url: string | null
  alert_whatsapp_number: string | null
  module_reviews: boolean
  module_alerts: boolean
}
```

From components/ui/MetricCard.tsx:
```typescript
interface MetricCardProps {
  label: string
  value: string | number
  icon?: React.ReactNode
  valueClassName?: string
  trend?: string  // shows as small gray text below value
}
// Usage: <MetricCard label="Rating medio" value="4.2" icon={<Star size={20} />} trend="▲ +0.3 vs mese scorso" />
```

From recharts (already installed, used in analytics/page.tsx):
```typescript
import {
  ComposedChart, Bar, Line, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer
} from 'recharts'
// ComposedChart with dual Y-axes: left for rating (1-5), right for count
// Data shape: [{ month: 'Gen 26', avgRating: 4.2, count: 12 }, ...]
```

Dashboard auth pattern (from analytics/page.tsx):
```typescript
'use client'
// useEffect → loadData() → supabase.auth.getUser() → if (!user) router.push('/login')
// .from('table').select('*').eq('merchant_id', merchantId)
```

API PATCH (from plan 16-01):
```typescript
// PATCH /api/ocio/reviews/[id]
// Headers: Authorization: Bearer {session.access_token}
// Body: { reply_status: 'replied' | 'ignored' }
// Response: 200 { success: true }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard OCIO — KPI, chart trend, lista filtrata</name>
  <files>app/dashboard/ocio/page.tsx</files>
  <action>
Riscrivi completamente `app/dashboard/ocio/page.tsx`. Il file attuale è uno stub di 62 righe — sostituirlo interamente.

**Struttura del componente:**

```typescript
'use client'

import { useEffect, useState, useMemo } from 'react'
import { createClient } from '@/lib/supabase'
import { useRouter } from 'next/navigation'
import { usePlan } from '@/lib/hooks/usePlan'
import UpgradePrompt from '@/components/ui/UpgradePrompt'
import EmptyState from '@/components/ui/EmptyState'
import MetricCard from '@/components/ui/MetricCard'
import type { OcioReview } from '@/lib/types'
import {
  Eye, Settings, Star, ThumbsUp, ThumbsDown, Minus,
  AlertTriangle, Clock, CheckCircle, XCircle, Copy, ExternalLink
} from 'lucide-react'
import {
  ComposedChart, Bar, Line, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer
} from 'recharts'
import Link from 'next/link'
```

**State:**
```typescript
const [reviews, setReviews] = useState<OcioReview[]>([])
const [googleMapsUrl, setGoogleMapsUrl] = useState<string | null>(null)
const [loading, setLoading] = useState(true)
const [selectedReview, setSelectedReview] = useState<OcioReview | null>(null)
const [filterSentiment, setFilterSentiment] = useState<'all' | 'positive' | 'neutral' | 'negative'>('all')
const [filterRating, setFilterRating] = useState<number | 'all'>('all')
const [filterPeriod, setFilterPeriod] = useState<'30' | '90' | 'all'>('30')
const [copying, setCopying] = useState(false)
const [accessToken, setAccessToken] = useState<string>('')
```

**loadData() — chiamata in useEffect:**
1. `supabase.auth.getUser()` → se no user: `router.push('/login')`, return
2. `supabase.auth.getSession()` → `setAccessToken(session.access_token)`
3. Leggi `profile.merchant_id` da profiles
4. `SELECT * FROM ocio_reviews WHERE merchant_id = $merchantId AND deleted_at IS NULL ORDER BY published_at DESC` — NO, `ocio_reviews` non ha `deleted_at`. Query: `.from('ocio_reviews').select('*').eq('merchant_id', merchantId).order('published_at', { ascending: false })`
5. Leggi anche `ocio_config` per `google_maps_url`: `.from('ocio_config').select('google_maps_url').eq('merchant_id', merchantId).single()` → `setGoogleMapsUrl(config?.google_maps_url ?? null)`
6. `setReviews(data ?? [])`
7. `setLoading(false)`

**KPI computation (useMemo da reviews):**
```typescript
const kpis = useMemo(() => {
  const now = new Date()
  const thirtyDaysAgo = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000)
  const sixtyDaysAgo = new Date(now.getTime() - 60 * 24 * 60 * 60 * 1000)

  const withRating = reviews.filter(r => r.rating !== null)
  const avgRating = withRating.length > 0
    ? (withRating.reduce((sum, r) => sum + (r.rating ?? 0), 0) / withRating.length).toFixed(1)
    : '—'

  // Trend rating: media ultimi 30gg vs 30-60gg
  const last30 = withRating.filter(r => r.published_at && new Date(r.published_at) >= thirtyDaysAgo)
  const prev30 = withRating.filter(r => {
    if (!r.published_at) return false
    const d = new Date(r.published_at)
    return d >= sixtyDaysAgo && d < thirtyDaysAgo
  })
  const avgLast30 = last30.length > 0 ? last30.reduce((sum, r) => sum + (r.rating ?? 0), 0) / last30.length : null
  const avgPrev30 = prev30.length > 0 ? prev30.reduce((sum, r) => sum + (r.rating ?? 0), 0) / prev30.length : null
  const ratingTrend = avgLast30 !== null && avgPrev30 !== null
    ? (avgLast30 - avgPrev30 >= 0 ? `▲ +${(avgLast30 - avgPrev30).toFixed(1)} vs mese scorso` : `▼ ${(avgLast30 - avgPrev30).toFixed(1)} vs mese scorso`)
    : undefined

  const newLast30 = reviews.filter(r => r.published_at && new Date(r.published_at) >= thirtyDaysAgo).length
  const pending = reviews.filter(r => r.reply_status === 'pending').length

  return { avgRating, total: reviews.length, newLast30, pending, ratingTrend }
}, [reviews])
```

**Chart data (useMemo — ultimi 6 mesi):**
```typescript
const chartData = useMemo(() => {
  const months: { month: string; avgRating: number | null; count: number }[] = []
  const now = new Date()
  for (let i = 5; i >= 0; i--) {
    const d = new Date(now.getFullYear(), now.getMonth() - i, 1)
    const label = d.toLocaleDateString('it-IT', { month: 'short', year: '2-digit' })
    const monthReviews = reviews.filter(r => {
      if (!r.published_at) return false
      const rd = new Date(r.published_at)
      return rd.getFullYear() === d.getFullYear() && rd.getMonth() === d.getMonth()
    })
    const withR = monthReviews.filter(r => r.rating !== null)
    const avg = withR.length > 0 ? parseFloat((withR.reduce((s, r) => s + (r.rating ?? 0), 0) / withR.length).toFixed(1)) : null
    months.push({ month: label, avgRating: avg, count: monthReviews.length })
  }
  return months
}, [reviews])
```

**Filtered reviews (useMemo):**
```typescript
const filteredReviews = useMemo(() => {
  const now = new Date()
  return reviews.filter(r => {
    if (filterSentiment !== 'all' && r.ai_sentiment !== filterSentiment) return false
    if (filterRating !== 'all' && r.rating !== filterRating) return false
    if (filterPeriod !== 'all' && r.published_at) {
      const days = parseInt(filterPeriod)
      const cutoff = new Date(now.getTime() - days * 24 * 60 * 60 * 1000)
      if (new Date(r.published_at) < cutoff) return false
    }
    return true
  })
}, [reviews, filterSentiment, filterRating, filterPeriod])
```

**Layout JSX:**

Header con titolo "OCIO — Reputation Intelligence" (Eye icon) + link Impostazioni a destra.

Sezione KPI: griglia `grid grid-cols-2 lg:grid-cols-4 gap-4`:
- `<MetricCard label="Rating medio" value={kpis.avgRating} icon={<Star size={20} />} trend={kpis.ratingTrend} />`
- `<MetricCard label="Recensioni totali" value={kpis.total} icon={<Eye size={20} />} />`
- `<MetricCard label="Nuove (30gg)" value={kpis.newLast30} icon={<Clock size={20} />} />`
- `<MetricCard label="Da rispondere" value={kpis.pending} icon={<AlertTriangle size={20} />} />`

Sezione chart (solo se `chartData.some(d => d.count > 0)`):
```tsx
<div className="bg-white border border-[#E8E8E8] rounded-xl p-6">
  <h2 className="text-sm font-semibold text-gray-700 mb-4">Trend ultimi 6 mesi</h2>
  <ResponsiveContainer width="100%" height={220}>
    <ComposedChart data={chartData}>
      <CartesianGrid strokeDasharray="3 3" stroke="#F0F0F0" />
      <XAxis dataKey="month" tick={{ fontSize: 12 }} />
      <YAxis yAxisId="left" domain={[1, 5]} tick={{ fontSize: 12 }} label={{ value: 'Rating', angle: -90, position: 'insideLeft', style: { fontSize: 11 } }} />
      <YAxis yAxisId="right" orientation="right" tick={{ fontSize: 12 }} label={{ value: 'Num.', angle: 90, position: 'insideRight', style: { fontSize: 11 } }} />
      <Tooltip />
      <Legend />
      <Bar yAxisId="right" dataKey="count" name="Recensioni" fill="#E5E7EB" radius={[4,4,0,0]} />
      <Line yAxisId="left" type="monotone" dataKey="avgRating" name="Rating medio" stroke="#111111" strokeWidth={2} dot={{ r: 3 }} connectNulls />
    </ComposedChart>
  </ResponsiveContainer>
</div>
```

Sezione filtri (flex wrap, gap-2):
- Sentiment: 4 bottoni pill (Tutti / Positive / Neutre / Negative) — attivo: bg-black text-white, inattivo: border border-gray-200 text-gray-600 hover:bg-gray-50
- Rating: 6 bottoni pill (Tutti / ⭐1 / ⭐⭐2 / ... / ⭐⭐⭐⭐⭐5) — stessa logica
- Periodo: 3 bottoni pill (Ultimi 30gg / 90gg / Tutto)

Lista recensioni filtrate (space-y-3):
- Se `reviews.length === 0`: `<EmptyState icon={<Eye size={48} />} title="Nessuna recensione ancora" description="Configura l'URL Google Maps nelle impostazioni OCIO e attendi il primo scraping automatico." action={<Link href="/dashboard/ocio/settings">...</Link>} />`
- Se `filteredReviews.length === 0` ma `reviews.length > 0`: card con testo "Nessuna recensione corrisponde ai filtri selezionati."
- Altrimenti: mappa `filteredReviews.map(review => <ReviewCard key={review.id} review={review} onClick={() => setSelectedReview(review)} />)`

**ReviewCard (componente inline o sub-function):**
```tsx
function ReviewCard({ review, onClick }: { review: OcioReview; onClick: () => void }) {
  // Stelle: Array(5).fill(0).map((_, i) => <Star size={14} className={i < (review.rating ?? 0) ? 'text-yellow-400 fill-yellow-400' : 'text-gray-200'} />)
  // Data: review.published_at → toLocaleDateString('it-IT', { day: 'numeric', month: 'short', year: 'numeric' })
  // Testo: line-clamp-3 (Tailwind)
  // Badge sentiment: verde (positive) / giallo (neutral) / rosso (negative) — piccoli pill text-xs
  // Badge urgenza: grigio (low) / giallo (medium) / arancio (high) / rosso (critical)
  // Temi: max 2 badge temi + "+N altri" se > 2
  // Pulsante: "Vedi risposta AI" → onClick()
  // reply_status badge: "Risposto" (verde, CheckCircle) / "Ignorato" (grigio, XCircle) / nessun badge per pending
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "ocio/page" | head -10</automated>
  </verify>
  <done>Pagina compila senza errori TypeScript; 4 MetricCard visibili; ComposedChart renderizzato; filtri sentiment/rating/periodo funzionanti con .filter() client-side; lista recensioni mostra cards con stelle, badge, testo troncato</done>
</task>

<task type="auto">
  <name>Task 2: Modal recensione — analisi AI + copia risposta + update reply_status</name>
  <files>app/dashboard/ocio/page.tsx</files>
  <action>
Aggiungi il modal e la logica `updateReplyStatus` a `app/dashboard/ocio/page.tsx` (stesso file del Task 1, continuazione).

**Funzione updateReplyStatus:**
```typescript
async function updateReplyStatus(reviewId: string, status: 'replied' | 'ignored') {
  try {
    await fetch(`/api/ocio/reviews/${reviewId}`, {
      method: 'PATCH',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${accessToken}`,
      },
      body: JSON.stringify({ reply_status: status }),
    })
    // Update local state — aggiorna la review nell'array
    setReviews(prev => prev.map(r =>
      r.id === reviewId
        ? { ...r, reply_status: status, ...(status === 'replied' ? { replied_at: new Date().toISOString() } : {}) }
        : r
    ))
    setSelectedReview(null) // chiudi modal
  } catch (err) {
    console.error('Failed to update reply status', err)
  }
}
```

**Funzione handleCopy:**
```typescript
async function handleCopy(text: string) {
  try {
    await navigator.clipboard.writeText(text)
    setCopying(true)
    setTimeout(() => setCopying(false), 2000)
  } catch {
    // fallback silenzioso
  }
}
```

**Modal JSX** — aggiunto dopo la lista recensioni, renderizzato quando `selectedReview !== null`:

```tsx
{selectedReview && (
  <div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4" onClick={() => setSelectedReview(null)}>
    <div className="bg-white rounded-xl max-w-2xl w-full max-h-[90vh] overflow-y-auto p-6 space-y-5" onClick={e => e.stopPropagation()}>
      {/* Header: stelle + autore + data + X */}
      {/* Testo completo della recensione */}
      {/* Se ai_is_fake: banner giallo-arancio con AlertTriangle + ai_fake_reason */}
      {/* Analisi AI: sentiment, urgenza (badge), score, temi (tutti i badge) */}
      {/* Risposta AI suggerita: textarea read-only o pre + pulsante Copia */}
      {/* Pulsante Copia: testo 'Copia risposta' → se copying: verde + 'Copiato!' */}
      {/* Link "Apri su Google Maps": usa review_url ?? googleMapsUrl, target="_blank" rel="noopener" */}
      {/* Se review_url null E googleMapsUrl null: non mostrare il link */}
      {/* Footer: pulsanti "Ho risposto" (verde) e "Ignora" (grigio) + pulsante Chiudi */}
      {/* "Ho risposto" → updateReplyStatus(selectedReview.id, 'replied') */}
      {/* "Ignora" → updateReplyStatus(selectedReview.id, 'ignored') */}
      {/* reply_status già 'replied' o 'ignored': mostrare stato, disabilitare pulsante corrispondente */}
    </div>
  </div>
)}
```

Dettagli implementazione modal:
- **Banner fake**: `bg-amber-50 border border-amber-200 rounded-lg p-3 flex gap-2` con `AlertTriangle className="text-amber-600"` e testo `ai_fake_reason`
- **Risposta AI**: `<pre className="bg-gray-50 rounded-lg p-4 text-sm text-gray-700 whitespace-pre-wrap font-sans">{selectedReview.ai_suggested_reply}</pre>`
- **Pulsante Copia**: `className={copying ? 'bg-green-600 text-white ...' : 'bg-[#111111] text-white ...'}` — usa `transition-colors`
- **Review senza risposta AI**: se `ai_suggested_reply` è null, mostrare `<p className="text-sm text-gray-400 italic">Analisi AI non ancora disponibile</p>`
- **Review senza testo**: se `text` è null/empty, mostrare `<p className="text-sm text-gray-400 italic">Nessun testo disponibile</p>`
- **Formato data nel modal**: `new Date(selectedReview.published_at).toLocaleDateString('it-IT', { day: 'numeric', month: 'long', year: 'numeric' })`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>TypeScript compila senza errori; modal appare al click su "Vedi risposta AI"; pulsante Copia diventa verde per 2s; "Ho risposto"/"Ignora" aggiornano reply_status via PATCH e chiudono il modal; la card nella lista riflette il nuovo reply_status (badge "Risposto"/"Ignorato")</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verifica visiva dashboard OCIO completa</name>
  <what-built>Dashboard OCIO completa: 4 KPI + ComposedChart trend 6 mesi + lista recensioni con filtri + modal con analisi AI + copia risposta + update reply_status via PATCH API</what-built>
  <how-to-verify>
    1. Vai su http://localhost:3000/dashboard/ocio (con account BUSINESS che ha recensioni scrappate)
    2. Verifica i 4 KPI nella riga superiore: rating medio, totale recensioni, nuove 30gg, da rispondere
    3. Se ci sono recensioni in più mesi, verifica che il grafico ComposedChart mostri barre + linea
    4. Testa i filtri: clicca "Negative" e verifica che la lista mostri solo recensioni negative; clicca su una stella per filtrare per rating
    5. Clicca su "Vedi risposta AI" su una card — verifica che il modal si apra con testo completo, badge sentiment/urgenza, temi, risposta AI
    6. Clicca "Copia risposta" — verifica che il pulsante diventi verde per 2 secondi
    7. Clicca "Ho risposto" — verifica che il modal si chiuda e la card mostri badge "Risposto"
    8. Verifica empty state: se non ci sono recensioni, viene mostrato il messaggio con link alle impostazioni
    9. Vai su /dashboard/ocio/settings → verifica che il campo "Numero WhatsApp per alert" sia visibile (se module_alerts è attivo)
  </how-to-verify>
  <resume-signal>Digita "approvato" oppure descrivi eventuali problemi da correggere</resume-signal>
  <action>Verifica manuale della dashboard OCIO nel browser. Seguire i passi in how-to-verify.</action>
  <verify>Utente conferma "approvato" dopo aver testato tutti i punti di verifica.</verify>
  <done>Dashboard OCIO funziona correttamente: KPI visibili, filtri operativi, modal apre e chiude, copia risposta funziona, reply_status aggiornato.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errori TypeScript
- `app/dashboard/ocio/page.tsx` — file esiste, > 300 righe, contiene `ComposedChart`, `selectedReview`, `filteredReviews`, `updateReplyStatus`, `handleCopy`
- `/dashboard/ocio` carica senza errori JS nella console del browser
- Filtri sentiment/rating/periodo aggiornano la lista in real-time
- Modal apre e chiude correttamente; reply_status aggiornato in DB dopo click "Ho risposto"
</verification>

<success_criteria>
- DASH-01: Lista recensioni con filtri sentiment, rating (1-5 stelle), periodo — funzionante client-side
- DASH-02: Modal con autore, rating, testo completo, data, sentiment, urgenza, temi, flag fake
- DASH-03: "Copia risposta" copia ai_suggested_reply negli appunti con feedback visivo 2s
- DASH-04: 4 KPI card + ComposedChart trend 6 mesi visibili nell'header della dashboard
- Feature gating: merchant non-BUSINESS vede UpgradePrompt
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-alert/16-02-SUMMARY.md`
</output>
